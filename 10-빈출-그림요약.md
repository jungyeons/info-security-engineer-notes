# 10. 빈출 그림요약

이 문서는 정보보안기사 필기 5과목과 실기 정보보안실무에서 시험에 자주 연결될 가능성이 높은 흐름을 그림 중심으로 정리한 빠른 회독용 자료입니다. 실제 문제는 회차마다 달라질 수 있으므로, 여기서는 공식 출제기준 안에서 반복적으로 묻기 좋은 구조와 함정 포인트를 모았습니다.

## 전체 출제 지도

```mermaid
flowchart TD
    A["정보보안기사"] --> B["시스템보안"]
    A --> C["네트워크보안"]
    A --> D["어플리케이션보안"]
    A --> E["정보보안일반"]
    A --> F["관리 및 법규"]
    A --> G["실기 정보보안실무"]

    B --> B1["계정/권한"]
    B --> B2["로그/패치"]
    B --> B3["악성코드/취약점"]

    C --> C1["OSI/TCP-IP"]
    C --> C2["스캔/스푸핑/스니핑/DoS"]
    C --> C3["방화벽/IDS/IPS/VPN"]

    D --> D1["Web 취약점"]
    D --> D2["FTP/메일/DNS/DB"]
    D --> D3["개발보안"]

    E --> E1["암호"]
    E --> E2["인증/접근통제"]
    E --> E3["PKI/전자서명"]

    F --> F1["위험관리"]
    F --> F2["인증제도"]
    F --> F3["개인정보/정보통신망 법규"]

    G --> G1["설정 점검"]
    G --> G2["로그 분석"]
    G --> G3["위험분석 답안"]
```

## A급 우선순위

| 과목 | 먼저 외울 것 | 이유 |
| --- | --- | --- |
| 시스템보안 | 계정, 권한, 로그, 원격접속, 패치 | 실기 설정 점검과 바로 연결 |
| 네트워크보안 | OSI, TCP 플래그, 포트, DoS/DDoS, 스푸핑 | 문제에서 로그/패킷 근거로 자주 꼬기 좋음 |
| 어플리케이션보안 | SQL 삽입, XSS, CSRF, 파일 업로드, DNS/메일 보안 | 공격명-원인-대응 연결이 명확함 |
| 정보보안일반 | 대칭키/공개키/해시/MAC/전자서명/PKI | 선택지 함정이 많음 |
| 관리 및 법규 | 위험관리, 사고대응, 개인정보보호 안전조치 | 실기 서술형으로 쓰기 좋음 |

## 시스템보안 그림

### 시스템 하드닝 흐름

```mermaid
flowchart LR
    A["자산 식별"] --> B["계정 점검"]
    B --> C["권한 점검"]
    C --> D["불필요 서비스 제거"]
    D --> E["패치 적용"]
    E --> F["로그 설정"]
    F --> G["백업/복구 확인"]
    G --> H["취약점 재점검"]
```

### 시스템 침해사고 흔한 흐름

```mermaid
flowchart TD
    A["초기 침투"] --> B["계정 탈취"]
    B --> C["권한 상승"]
    C --> D["내부 이동"]
    D --> E["정보 수집"]
    E --> F["자료 반출"]
    F --> G["로그 삭제/은닉"]

    A --> A1["피싱, 취약 서비스"]
    C --> C1["SUID, 관리자 권한, 패치 미흡"]
    G --> G1["감사 로그, EDR, FIM 확인"]
```

### 운영체제 점검 포인트

| 점검 | 윈도우 | 리눅스/유닉스 |
| --- | --- | --- |
| 계정 | `net user`, 관리자 그룹 | `/etc/passwd`, `/etc/shadow`, UID 0 |
| 권한 | NTFS 권한, 공유 권한 | rwx, SUID, SGID, Sticky bit |
| 로그 | 이벤트 뷰어 | `/var/log/*`, `journalctl` |
| 원격접속 | RDP, WinRM | SSH, sudo |
| 예약작업 | 작업 스케줄러 | cron, at |

### 실기 답안 공식

```text
취약 상태: [설정/로그/권한 문제]
위험: [비인가 접근/권한 상승/정보유출/추적 실패]
조치: [구체 설정 변경]
확인: [명령어/로그/재점검]
재발 방지: [정기 점검/승인 절차/모니터링]
```

## 네트워크보안 그림

### OSI 계층별 공격 연결

```mermaid
flowchart TD
    L7["7 응용: HTTP, DNS, SMTP"] --> A7["웹 공격, DNS 공격, 스팸/피싱"]
    L4["4 전송: TCP/UDP"] --> A4["SYN Flood, UDP Flood, 포트 스캔"]
    L3["3 네트워크: IP, ICMP"] --> A3["IP Spoofing, Smurf, ICMP Flood"]
    L2["2 데이터링크: Ethernet, ARP"] --> A2["ARP Spoofing, MAC Flooding"]
    L1["1 물리"] --> A1["도청, 회선 장애, 물리 침입"]
```

### TCP 연결과 SYN Flood

```mermaid
sequenceDiagram
    participant C as Client
    participant S as Server
    C->>S: SYN
    S-->>C: SYN/ACK
    C->>S: ACK
    Note over C,S: 정상 연결 수립
```

```mermaid
sequenceDiagram
    participant A as Attacker
    participant S as Server
    A->>S: SYN 대량 전송
    S-->>A: SYN/ACK 응답
    Note over A,S: 마지막 ACK가 오지 않아 반쪽 연결 증가
```

### 스푸핑/스니핑/하이재킹 관계

```mermaid
flowchart LR
    A["ARP 스푸핑"] --> B["중간자 위치 확보"]
    B --> C["스니핑"]
    C --> D["세션 쿠키/인증정보 탈취"]
    D --> E["세션 하이재킹"]
    B --> F["패킷 변조"]
```

### 보안장비 위치 감각

```mermaid
flowchart LR
    I["인터넷"] --> FW["방화벽"]
    FW --> WAF["WAF"]
    WAF --> WEB["웹 서버"]
    WEB --> WAS["WAS"]
    WAS --> DB["DB"]
    FW --> IDS["IDS/IPS"]
    WEB --> LOG["로그 수집/SIEM"]
    DB --> LOG
    FW --> LOG
```

### 공격별 바로 대응

| 공격 | 로그/패킷 근거 | 1차 대응 | 재발 방지 |
| --- | --- | --- | --- |
| 포트 스캔 | 다수 포트 순차 접근 | 차단, 탐지 룰 적용 | 외부 노출 포트 최소화 |
| SYN Flood | SYN 급증, 반쪽 연결 | SYN Cookies, rate limit | DDoS 대응장비, 임계치 모니터링 |
| ARP 스푸핑 | ARP 테이블 변경 | 공격 단말 격리 | 포트 보안, 정적 ARP |
| DNS 증폭 | UDP 53 대량 응답 | 오픈 리졸버 차단 | 재귀 질의 제한 |
| 원격접속 공격 | 로그인 실패 반복 | 계정 잠금, IP 차단 | MFA, VPN, 허용 IP 제한 |

## 어플리케이션보안 그림

### 웹 요청 처리와 취약점 위치

```mermaid
flowchart LR
    U["사용자 입력"] --> V["입력값 검증"]
    V --> A["애플리케이션 로직"]
    A --> DB["DB 질의"]
    A --> F["파일 처리"]
    A --> O["화면 출력"]

    U -.검증 미흡.-> SQL["SQL 삽입"]
    U -.검증 미흡.-> CMD["명령 삽입"]
    F -.검증 미흡.-> UP["웹셸 업로드"]
    O -.인코딩 미흡.-> XSS["XSS"]
```

### SQL 삽입 흐름

```mermaid
flowchart LR
    A["공격 입력"] --> B["SQL 문자열에 직접 결합"]
    B --> C["쿼리 구조 변경"]
    C --> D["인증 우회/정보 조회/데이터 변조"]
    D --> E["대응: Prepared Statement, 최소권한, 오류 제한"]
```

### XSS와 CSRF 차이

| 구분 | XSS | CSRF |
| --- | --- | --- |
| 핵심 | 스크립트가 피해자 브라우저에서 실행 | 피해자가 인증된 상태로 원치 않는 요청 |
| 원인 | 출력 인코딩 미흡 | 요청 위조 방어 미흡 |
| 피해 | 세션 탈취, 피싱 | 비밀번호 변경, 결제, 글쓰기 |
| 대응 | 출력 인코딩, CSP, HttpOnly | CSRF 토큰, SameSite, 재인증 |

### 파일 업로드 공격

```mermaid
flowchart TD
    A["공격자가 웹셸 업로드"] --> B{"확장자/내용 검증?"}
    B -->|미흡| C["웹 루트에 저장"]
    C --> D{"실행 권한 있음?"}
    D -->|예| E["원격 명령 실행"]
    B -->|정상 검증| F["차단 또는 안전 저장"]
    D -->|아니오| F
```

### DNS 보안

```mermaid
flowchart TD
    A["DNS 서버"] --> B["영역전송 제한"]
    A --> C["재귀 질의 제한"]
    A --> D["DNSSEC"]
    A --> E["캐시 오염 방지"]
    A --> F["DNS 로그 모니터링"]
```

## 정보보안일반 그림

### 접근통제 모델 선택

```mermaid
flowchart TD
    A["접근통제"] --> B["DAC"]
    A --> C["MAC"]
    A --> D["RBAC"]
    A --> E["ABAC"]
    B --> B1["소유자가 권한 부여"]
    C --> C1["보안 등급 중심"]
    D --> D1["직무 역할 중심"]
    E --> E1["속성/환경 조건 중심"]
```

### 암호학은 09번 파일

암호학은 [09-암호학-심화-그림정리.md](09-암호학-심화-그림정리.md)에 별도 확장했습니다. 시험 직전에는 다음 5개 그림을 우선 봅니다.

- 암호학 전체 지도
- 하이브리드 암호
- 전자서명 흐름
- 인증서 검증 흐름
- 블록 암호 운영 모드

## 관리 및 법규 그림

### 위험관리 전체 흐름

```mermaid
flowchart LR
    A["범위 정의"] --> B["자산 식별"]
    B --> C["위협 식별"]
    C --> D["취약점 식별"]
    D --> E["가능성/영향 평가"]
    E --> F["위험도 산정"]
    F --> G["대책 선정"]
    G --> H["이행계획"]
    H --> I["잔여위험 승인"]
```

### 위험처리 전략

```mermaid
flowchart TD
    A["위험처리"] --> B["감소"]
    A --> C["회피"]
    A --> D["전가"]
    A --> E["수용"]
    B --> B1["패치, 접근통제, 암호화"]
    C --> C1["위험한 서비스 중단"]
    D --> D1["보험, 계약"]
    E --> E1["잔여위험 승인"]
```

### 침해사고 대응

```mermaid
flowchart LR
    A["준비"] --> B["탐지/분석"]
    B --> C["확산 차단"]
    C --> D["제거"]
    D --> E["복구"]
    E --> F["사후 개선"]
    F --> A
```

### 디지털 포렌식 핵심

```mermaid
flowchart TD
    A["증거 수집"] --> B["원본 보존"]
    B --> C["해시로 무결성 확인"]
    C --> D["분석 사본 생성"]
    D --> E["분석 과정 기록"]
    E --> F["연계보관성 유지"]
```

### 개인정보 처리 흐름

```mermaid
flowchart LR
    A["수집"] --> B["이용"]
    B --> C["제공/위탁"]
    C --> D["보관"]
    D --> E["파기"]
    A --> A1["목적 명확화/최소 수집"]
    D --> D1["접근통제/암호화/접속기록"]
    E --> E1["복구 불가능하게 파기"]
```

## 실기 종합 답안 구조

```mermaid
flowchart TD
    A["문제 상황"] --> B["이상 징후 식별"]
    B --> C["원인/취약점 추정"]
    C --> D["위험 설명"]
    D --> E["즉시 조치"]
    E --> F["재발 방지"]
    F --> G["확인 방법"]
```

## 과목별 마지막 체크

### 시스템보안

- 불필요 계정과 기본 계정 제거
- 관리자 권한 최소화
- 원격접속 제한과 MFA
- 로그 활성화와 중앙 수집
- 패치와 취약점 조치 이력
- 백업과 복구 시험

### 네트워크보안

- OSI 계층별 장비와 프로토콜
- TCP 3-way handshake와 플래그
- 포트 스캔, 스푸핑, 스니핑, DoS/DDoS
- 방화벽 정책 검토
- IDS/IPS 탐지와 차단 차이
- VPN, NAT, VLAN, NAC

### 어플리케이션보안

- FTP 익명 접속과 평문 전송
- 메일 오픈 릴레이, SPF/DKIM/DMARC
- 웹 취약점 원인과 대응
- DNS 영역전송과 재귀 질의 제한
- DB 계정 최소권한과 암호화
- 개발보안: 입력검증, 출력인코딩, 인증/인가

### 정보보안일반

- 대칭키와 공개키 차이
- 해시, MAC, 전자서명 차이
- 접근통제 모델
- PKI 구성요소와 인증서 검증
- 암호 운영 모드

### 관리 및 법규

- 위험 = 위협이 취약점을 이용해 자산에 영향을 주는 가능성
- 위험처리: 감소, 회피, 전가, 수용
- 보호대책: 관리적, 물리적, 기술적
- 사고대응: 준비, 탐지/분석, 차단, 제거, 복구, 사후개선
- 개인정보 안전성 확보조치

