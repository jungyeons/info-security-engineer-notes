# 17. 헷갈리는 개념 완전정리

정보보안기사 문제는 모르는 개념보다 `비슷한 개념을 헷갈리게 하는 선택지`에서 많이 틀립니다. 이 문서는 시험장에서 헷갈리기 쉬운 개념들을 문장으로 구분할 수 있게 정리한 자료입니다.

## 인증, 인가, 식별, 책임추적

식별은 사용자가 자신이 누구인지 주장하는 단계입니다. 예를 들어 ID를 입력하는 것이 식별입니다. 인증은 그 주장이 맞는지 확인하는 단계입니다. 비밀번호, OTP, 인증서, 생체정보가 인증 수단입니다. 인가는 인증된 사용자가 어떤 자원에 어떤 행위를 할 수 있는지 결정하는 단계입니다. 책임추적은 이후 누가 어떤 행위를 했는지 로그로 확인할 수 있게 하는 것입니다.

시험에서 `로그인`이라는 말만 나오면 식별과 인증을 함께 떠올리고, `권한 확인`이라는 말이 나오면 인가를 떠올립니다. 관리자 작업 기록, 감사 로그, 개인별 계정은 책임추적성과 관련됩니다.

## 기밀성, 무결성, 가용성

기밀성은 허가된 사람만 볼 수 있게 하는 성질입니다. 암호화와 접근통제가 대표 대책입니다. 무결성은 허가 없이 변경되지 않았음을 보장하는 성질입니다. 해시, MAC, 전자서명, 파일무결성 점검이 관련됩니다. 가용성은 필요한 시점에 서비스를 사용할 수 있게 하는 성질입니다. 이중화, 백업, DDoS 대응, 재해복구가 관련됩니다.

암호화는 기밀성을 주로 제공합니다. 해시는 무결성을 확인합니다. 백업은 가용성과 복구에 가깝습니다.

## 암호화, 인코딩, 해싱

암호화는 키를 이용해 평문을 암호문으로 바꾸고, 복호화를 통해 원문을 되돌릴 수 있습니다. 인코딩은 데이터 표현 방식을 바꾸는 것으로 보안 기능이 아닙니다. Base64는 인코딩이지 암호화가 아닙니다. 해싱은 원문을 고정 길이 요약값으로 바꾸며 일반적으로 복호화하지 않습니다.

시험에서 `Base64로 암호화`라는 표현이 나오면 틀린 표현입니다. `비밀번호를 암호화하여 저장`도 주의해야 합니다. 비밀번호는 복호화 가능한 암호화보다 Salt와 느린 해시를 이용해 저장하는 것이 안전합니다.

## 해시, MAC, 전자서명

해시는 키가 없습니다. 그래서 무결성 확인에는 쓰이지만 누가 만들었는지 인증하기 어렵습니다. MAC은 공유 비밀키를 사용하므로 메시지 무결성과 송신자 인증을 제공합니다. 하지만 송수신자가 같은 키를 공유하므로 제3자에 대한 부인방지는 약합니다. 전자서명은 서명자의 개인키와 공개키를 사용하여 무결성, 인증, 부인방지를 제공합니다.

정리하면 `해시 = 무결성`, `MAC = 무결성 + 공유키 기반 인증`, `전자서명 = 무결성 + 인증 + 부인방지`입니다.

## 수신자 공개키와 송신자 개인키

공개키 암호에서 기밀성을 원하면 수신자의 공개키로 암호화합니다. 그래야 수신자의 개인키로만 복호화할 수 있습니다. 전자서명을 원하면 송신자의 개인키로 서명합니다. 그래야 누구나 송신자의 공개키로 검증할 수 있습니다.

시험장에서 헷갈리면 이렇게 외웁니다.

- 숨길 때: 받을 사람 공개키
- 증명할 때: 보낸 사람 개인키

## 대칭키와 공개키

대칭키는 같은 키를 공유합니다. 빠르지만 키 분배가 어렵습니다. 공개키는 공개키와 개인키 쌍을 사용합니다. 키 분배와 전자서명에 유리하지만 느립니다. 실제 TLS 같은 프로토콜은 공개키 방식으로 세션키를 안전하게 합의하고, 실제 데이터는 대칭키로 빠르게 암호화합니다.

`공개키가 대칭키보다 빠르다`는 선택지는 틀립니다. `대용량 데이터는 공개키로 암호화하는 것이 일반적이다`도 틀린 설명입니다.

## IDS, IPS, 방화벽, WAF

방화벽은 정책 기반 허용/차단 장비입니다. 주로 IP, 포트, 프로토콜, 방향을 기준으로 합니다. IDS는 침입을 탐지하고 경고합니다. IPS는 침입을 탐지하고 차단까지 수행합니다. WAF는 웹 애플리케이션 공격을 탐지/차단합니다.

구분 포인트:

- 방화벽: 네트워크 접근통제
- IDS: 탐지 중심
- IPS: 탐지 + 차단
- WAF: SQL 삽입, XSS, 파일 업로드 같은 웹 공격 대응

## 오탐과 미탐

오탐은 정상 행위를 공격으로 잘못 판단하는 것입니다. 미탐은 실제 공격을 놓치는 것입니다. 보안장비는 오탐이 너무 많으면 운영자가 경고를 무시하게 되고, 미탐이 많으면 공격을 놓칩니다. 그래서 탐지 정책 튜닝과 예외 정책 관리가 중요합니다.

## DoS와 DDoS

DoS는 단일 또는 제한된 공격원이 서비스를 마비시키는 공격입니다. DDoS는 다수의 분산된 공격원이 동시에 공격합니다. 대응도 다릅니다. DoS는 서버나 방화벽 수준에서 완화할 수 있는 경우가 있지만, DDoS는 대역폭 자체가 고갈될 수 있어 DDoS 대응장비, CDN, 클라우드 우회 방어, ISP 연계가 필요합니다.

## 스푸핑, 스니핑, 스니핑 이후 공격

스푸핑은 속이는 것입니다. ARP 스푸핑은 MAC 주소를 속이고, IP 스푸핑은 출발지 IP를 속이고, DNS 스푸핑은 DNS 응답을 속입니다. 스니핑은 훔쳐보는 것입니다. 스푸핑으로 중간자 위치를 확보하면 스니핑이 가능해지고, 스니핑으로 세션 쿠키를 얻으면 세션 하이재킹으로 이어질 수 있습니다.

## NAT와 방화벽

NAT는 주소 변환 기술입니다. 내부 사설 IP를 공인 IP로 변환하거나 포트 주소 변환을 수행합니다. NAT가 내부 주소를 숨기는 효과는 있지만 보안정책을 대신하지 않습니다. 방화벽은 명시적인 접근 허용/차단 정책을 적용합니다.

따라서 `NAT를 적용했으므로 방화벽이 필요 없다`는 틀린 설명입니다.

## VPN과 TLS

VPN은 공용망 위에 암호화된 사설 통신 경로를 만드는 기술입니다. IPsec VPN은 네트워크 계층 보호에 가깝고, SSL/TLS VPN은 응용 접근에 많이 쓰입니다. TLS는 웹 통신 등에서 서버 인증, 키 교환, 암호화, 무결성 보호를 제공합니다.

VPN을 사용해도 계정이 탈취되면 내부망 접근 경로가 됩니다. 그래서 VPN에는 MFA, 접속 로그, 단말 보안상태 점검이 필요합니다.

## HTTP와 HTTPS

HTTP는 평문 통신입니다. 중간에서 내용을 볼 수 있고 변조될 수 있습니다. HTTPS는 HTTP 위에 TLS를 적용하여 전송 구간을 보호합니다. HTTPS는 서버 인증서 검증이 중요합니다. 인증서 오류를 무시하면 중간자 공격에 취약해집니다.

HTTPS가 적용되어도 웹 애플리케이션 취약점이 사라지는 것은 아닙니다. SQL 삽입, XSS, 인증 우회는 HTTPS와 별개로 발생할 수 있습니다.

## XSS와 CSRF

XSS는 공격자가 삽입한 스크립트가 피해자 브라우저에서 실행되는 공격입니다. 대응은 출력 인코딩과 CSP입니다. CSRF는 피해자가 로그인된 상태에서 공격자가 의도한 요청을 보내게 하는 공격입니다. 대응은 CSRF 토큰, SameSite 쿠키, 중요 기능 재인증입니다.

XSS는 스크립트 실행, CSRF는 요청 위조입니다.

## SQL 삽입과 명령 삽입

SQL 삽입은 데이터베이스 쿼리 구조를 바꾸는 공격입니다. Prepared Statement가 핵심 대응입니다. 명령 삽입은 운영체제 명령에 사용자 입력이 섞여 명령이 실행되는 공격입니다. 운영체제 명령 호출을 피하고 안전한 API를 사용하며 인자를 검증해야 합니다.

## 인증과 세션

인증은 사용자가 누구인지 확인하는 것입니다. 세션은 인증 상태를 유지하는 방법입니다. 로그인 성공 후 세션ID를 발급하고, 이후 요청에서 세션ID로 사용자를 식별합니다. 세션ID가 탈취되면 공격자가 사용자처럼 행동할 수 있으므로 HTTPS, Secure, HttpOnly, SameSite, 세션 만료, 로그인 후 세션 재발급이 중요합니다.

## 접근통제 모델

DAC는 소유자가 권한을 정합니다. MAC은 보안 등급에 따라 중앙 정책이 강제됩니다. RBAC는 역할에 권한을 부여합니다. ABAC는 사용자, 자원, 환경의 속성을 조합합니다.

문제에서 `직무`, `역할`, `부서 업무`가 나오면 RBAC입니다. `기밀등급`, `보안등급`, `강제`가 나오면 MAC입니다. `소유자가 권한 부여`가 나오면 DAC입니다. `시간, 위치, 단말 상태`가 나오면 ABAC입니다.

## Bell-LaPadula와 Biba

Bell-LaPadula는 기밀성 모델입니다. 높은 등급 정보를 낮은 등급으로 흘러가지 않게 막습니다. Biba는 무결성 모델입니다. 낮은 무결성의 정보가 높은 무결성 영역을 오염시키지 않게 막습니다.

시험 암기:

- Bell-LaPadula: 기밀성
- Biba: 무결성

## 위험, 위협, 취약점

자산은 보호할 대상입니다. 위협은 피해를 줄 수 있는 원인입니다. 취약점은 위협이 이용할 수 있는 약점입니다. 위험은 위협이 취약점을 이용해 자산에 손실을 줄 가능성과 영향입니다.

예시:

- 자산: 고객 DB
- 위협: 해커의 SQL 삽입 공격
- 취약점: Prepared Statement 미적용
- 위험: 고객정보 유출 가능성과 그 영향

## 위험처리 전략

위험감소는 보호대책으로 위험도를 낮춥니다. 위험회피는 위험한 활동 자체를 중단합니다. 위험전가는 보험이나 계약으로 손실 부담을 이전합니다. 위험수용은 잔여위험을 승인하고 받아들입니다.

선택지에서 `서비스 중단`, `사업 철수`는 회피입니다. `보험 가입`, `위탁 계약`은 전가입니다. `패치`, `암호화`, `접근통제`는 감소입니다.

## BCP, DRP, RTO, RPO

BCP는 업무연속성계획입니다. 업무가 중단되어도 핵심 업무를 지속하기 위한 계획입니다. DRP는 재해복구계획입니다. 정보시스템 복구에 초점이 있습니다. RTO는 목표 복구 시간입니다. 얼마나 빨리 복구할 것인가입니다. RPO는 목표 복구 시점입니다. 어느 시점의 데이터까지 복구할 것인가입니다.

예를 들어 RTO가 4시간이면 4시간 안에 서비스를 복구해야 합니다. RPO가 1시간이면 최대 1시간 전 데이터까지 손실을 허용한다는 뜻입니다.

## 개인정보, 가명정보, 익명정보

개인정보는 살아 있는 개인을 알아볼 수 있는 정보입니다. 가명정보는 추가정보 없이는 특정 개인을 알아보기 어렵게 처리한 정보입니다. 익명정보는 더 이상 개인을 알아볼 수 없도록 처리한 정보입니다.

가명정보는 여전히 보호 대상입니다. 익명정보는 개인정보로 보기 어렵지만, 실제 익명성이 깨질 수 없도록 충분한 조치가 필요합니다.

## 민감정보와 고유식별정보

민감정보는 사상, 신념, 건강, 생체정보처럼 사생활 침해 우려가 큰 정보입니다. 고유식별정보는 주민등록번호, 여권번호, 운전면허번호, 외국인등록번호처럼 개인을 고유하게 식별하는 번호입니다.

시험에서는 둘을 섞어 냅니다. 건강정보는 민감정보이고, 주민등록번호는 고유식별정보입니다.

## ISMS-P와 제품 인증

ISMS-P는 조직의 정보보호 및 개인정보보호 관리체계를 인증하는 제도입니다. 조직 운영, 위험관리, 보호대책, 개인정보 처리 단계별 보호조치를 봅니다. 제품 인증은 특정 보안 제품의 보안기능과 보증 수준을 평가하는 것입니다.

조직 관리체계는 ISMS-P, 제품 자체의 보안성은 제품 인증으로 구분합니다.

## 포렌식과 로그분석

로그분석은 시스템과 보안장비에 남은 기록을 분석하는 것입니다. 디지털 포렌식은 증거를 법적/절차적으로 수집, 보존, 분석, 보고하는 과정입니다. 포렌식에서는 원본 보존, 무결성, 연계보관성이 중요합니다.

사고 대응에서 로그를 보는 것은 분석이고, 증거능력을 유지하며 수집/보존하는 것은 포렌식 관점입니다.

## 마지막 구분표

| 헷갈리는 쌍 | 구분 문장 |
| --- | --- |
| 인증/인가 | 인증은 신원 확인, 인가는 권한 확인 |
| 암호화/해시 | 암호화는 복호화 가능, 해시는 복호화 목적 아님 |
| MAC/전자서명 | MAC은 공유키, 전자서명은 개인키/공개키 |
| IDS/IPS | IDS는 탐지, IPS는 차단까지 |
| XSS/CSRF | XSS는 스크립트 실행, CSRF는 요청 위조 |
| NAT/방화벽 | NAT는 주소 변환, 방화벽은 접근통제 |
| BCP/DRP | BCP는 업무 지속, DRP는 시스템 복구 |
| RTO/RPO | RTO는 시간, RPO는 데이터 시점 |
| 위협/취약점 | 위협은 원인, 취약점은 약점 |
| ISMS-P/제품 인증 | ISMS-P는 조직 체계, 제품 인증은 제품 보안성 |

