# 12. 공격·취약점·대응 매트릭스

이 문서는 공격명만 외우는 것을 막기 위해 `원인 -> 증거 -> 영향 -> 대응 -> 재발방지`를 한 줄로 연결한 자료입니다. 필기 선택지와 실기 필답형 모두에 쓰기 좋게 구성했습니다.

## 전체 대응 흐름

```mermaid
flowchart LR
    A["탐지"] --> B["분류"]
    B --> C["영향범위 파악"]
    C --> D["격리/차단"]
    D --> E["원인 제거"]
    E --> F["복구"]
    F --> G["재발방지"]
    G --> H["보고/이력관리"]
```

## 시스템 공격

| 항목 | 원인 | 증거 | 영향 | 대응 | 재발방지 |
| --- | --- | --- | --- | --- | --- |
| 암호 대입 | 약한 암호, 잠금정책 미흡 | 로그인 실패 반복 | 계정 탈취 | 계정 잠금, IP 차단 | MFA, 암호정책 |
| 권한 상승 | 패치 미흡, SUID, sudo 오류 | 관리자 명령 실행 | 시스템 장악 | 계정 격리, 취약점 패치 | 최소권한 |
| 웹셸 | 업로드 검증 미흡 | 웹 루트 의심 파일 | 원격 명령 실행 | 파일 제거, 서버 격리 | 업로드 통제 |
| 랜섬웨어 | 악성메일, 취약 서비스 | 파일 확장자 변경, 대량 암호화 | 가용성 상실 | 격리, 복구 | 오프라인 백업, EDR |
| 루트킷 | 최고권한 침해 | 명령 결과 불일치 | 은닉 | 포렌식, 재설치 검토 | 무결성 점검 |
| 로그 삭제 | 은닉 시도 | 로그 공백, 서비스 중단 | 추적 실패 | 중앙 로그 확인 | 로그 원격전송 |
| 백도어 | 악성 프로세스, 계정 생성 | 의심 포트, 새 계정 | 지속 접근 | 차단, 계정 삭제 | FIM, EDR |
| 내부자 유출 | 과다 권한 | 대량 복사, 비업무 접근 | 정보유출 | 권한 회수, 조사 | DLP, 권한검토 |

## 네트워크 공격

| 항목 | 원인 | 증거 | 영향 | 대응 | 재발방지 |
| --- | --- | --- | --- | --- | --- |
| 포트 스캔 | 외부 노출 서비스 | 다수 포트 접근 | 취약점 탐색 | 차단, 탐지 | 노출 최소화 |
| SYN Flood | TCP 반쪽 연결 악용 | SYN 급증 | 서비스 지연 | SYN cookie | DDoS 대응 |
| UDP Flood | 대량 UDP | PPS 급증 | 대역폭 고갈 | rate limit | UDP 서비스 제한 |
| DNS 증폭 | 오픈 리졸버 | 큰 DNS 응답 | DDoS | 재귀 제한 | DNS 설정 점검 |
| ARP 스푸핑 | ARP 인증 부재 | 게이트웨이 MAC 변경 | 도청/변조 | 단말 격리 | DAI, 포트보안 |
| DNS 캐시 오염 | 취약 DNS | 잘못된 응답 | 피싱/우회 | 캐시 삭제, 패치 | DNSSEC |
| 세션 하이재킹 | 세션 탈취 | IP/UA 변경 | 계정 도용 | 세션 폐기 | HTTPS, 쿠키 보호 |
| 무선 침입 | 약한 암호화 | 미인가 AP/단말 | 내부망 접근 | 차단, 키 변경 | WPA3, 분리망 |

## 웹/애플리케이션 공격

| 항목 | 원인 | 증거 | 영향 | 대응 | 재발방지 |
| --- | --- | --- | --- | --- | --- |
| SQL 삽입 | 쿼리 직접 결합 | `' or 1=1`, DB 오류 | 정보유출/변조 | 차단, 쿼리 수정 | Prepared Statement |
| XSS | 출력 인코딩 미흡 | `<script>` 요청 | 세션 탈취 | 악성 입력 제거 | 출력 인코딩 |
| CSRF | 토큰 없음 | 사용자 의도 없는 요청 | 권한 오남용 | 세션 확인 | CSRF 토큰 |
| SSRF | URL 입력 검증 미흡 | 내부 IP 요청 | 내부망 접근 | 요청 차단 | 허용 목록 |
| 파일 업로드 | 확장자/실행권한 미흡 | 업로드 후 실행 | 웹셸 | 파일 제거 | 저장위치 분리 |
| 경로 조작 | 경로 정규화 미흡 | `../` 요청 | 파일 열람 | 차단 | 허용 목록 |
| 명령 삽입 | OS 명령 직접 호출 | `;`, `&&` 포함 | 명령 실행 | 기능 중지 | API 사용 |
| 인증 우회 | 서버 검증 미흡 | 비정상 권한 접근 | 정보노출 | 권한검사 수정 | 서버측 인가 |
| API 과다노출 | 응답 제한 미흡 | 불필요 필드 응답 | 개인정보 노출 | 응답 축소 | DTO/스키마 통제 |

## 메일/DNS/DB 공격

| 영역 | 공격/취약점 | 증거 | 대응 |
| --- | --- | --- | --- |
| 메일 | 오픈 릴레이 | 외부 도메인 릴레이 성공 | 릴레이 허용 대상 제한 |
| 메일 | 발신자 위조 | SPF/DKIM 실패 | SPF/DKIM/DMARC |
| 메일 | 악성 첨부 | 실행파일 첨부, URL 클릭 | 샌드박스, 교육 |
| DNS | 영역전송 | AXFR 성공 | 허용 IP 제한 |
| DNS | 터널링 | 긴 무작위 도메인 | 질의 분석, 차단 |
| DB | 기본 계정 | 기본 계정 로그인 | 계정 삭제/잠금 |
| DB | 과다 권한 | 앱 계정 DBA 권한 | 최소권한 |
| DB | 평문 저장 | 중요 컬럼 평문 | 암호화/토큰화 |
| DB | 백업 노출 | 백업 파일 웹 공개 | 저장소 분리/암호화 |

## 클라우드/가상화 공격

| 항목 | 원인 | 영향 | 대응 |
| --- | --- | --- | --- |
| 공개 버킷 | 저장소 공개 설정 | 대량 정보유출 | 공개 차단, 정책 점검 |
| 과다 IAM | 관리자 권한 남용 | 자원 장악 | 역할 분리, 최소권한 |
| 보안그룹 오류 | 0.0.0.0/0 관리포트 | 침입 경로 | 관리포트 제한 |
| 키 노출 | 코드/로그에 키 저장 | API 악용 | 키 폐기/재발급 |
| 스냅샷 공개 | 이미지/백업 공개 | 데이터 유출 | 공유 범위 점검 |
| 메타데이터 SSRF | 내부 메타데이터 접근 | 임시자격 탈취 | IMDSv2, SSRF 차단 |
| 컨테이너 권한 과다 | privileged 실행 | 호스트 침해 | 권한 제한 |

## 취약점 점검 보고서 템플릿

```text
취약점명:
대상 자산:
발견 일시:
위험도:
현황/증거:
영향:
조치 방안:
조치 담당자:
조치 기한:
조치 결과:
재점검 결과:
잔여위험:
```

## 실기 답안 단골 표현

- 공격 출발지 IP를 차단하고 동일 패턴 탐지 룰을 적용한다.
- 취약 서비스는 임시 중지 후 패치와 설정 변경을 수행한다.
- 침해 의심 시스템은 네트워크에서 격리하되 증거 보존을 위해 전원을 즉시 끄지 않는다.
- 관리자 계정과 서비스 계정의 암호를 변경하고 세션을 종료한다.
- 로그, 프로세스, 네트워크 연결, 파일 변경 내역을 수집한다.
- 원인 제거 후 백업에서 복구하고 정상 여부를 모니터링한다.
- 재발 방지를 위해 정책, 접근권한, 탐지 룰, 교육, 정기점검을 개선한다.

