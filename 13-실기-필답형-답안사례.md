# 13. 실기 필답형 답안사례

실기는 `정답 키워드`를 아는 것만큼 문장 구성도 중요합니다. 이 문서는 실제 답안처럼 쓸 수 있는 형태로 사례를 정리했습니다.

## 답안 만능 구조

```mermaid
flowchart LR
    A["현상"] --> B["원인/취약점"]
    B --> C["위험"]
    C --> D["즉시 조치"]
    D --> E["재발 방지"]
    E --> F["확인 방법"]
```

## 시스템보안 답안사례

### root 원격 로그인 허용

```text
root 계정의 원격 로그인이 허용되어 있으면 공격자가 암호 대입이나 탈취된 인증정보로 최고권한을 직접 획득할 수 있다.
SSH 설정에서 root 직접 로그인을 금지하고 일반 계정으로 접속한 뒤 sudo를 통해 필요한 명령만 수행하도록 한다.
접속 허용 IP를 제한하고 공개키 인증과 MFA를 적용하며, 인증 로그에서 비정상 접속 여부를 확인한다.
```

### SUID 파일 과다

```text
불필요한 SUID 파일은 일반 사용자가 파일 소유자의 권한으로 프로그램을 실행하게 하므로 권한 상승에 악용될 수 있다.
SUID가 필요한 파일인지 검토하고 불필요한 SUID 권한은 제거한다.
변경 후 파일 권한 목록과 무결성 점검 결과를 확인하고 정기적으로 SUID 파일을 점검한다.
```

### 공유폴더 Everyone 쓰기 허용

```text
공유폴더에 Everyone 쓰기 권한이 있으면 권한 없는 사용자가 중요 파일을 열람, 수정, 삭제하거나 악성 파일을 저장할 수 있다.
업무상 필요한 사용자와 그룹에만 최소 권한을 부여하고 공유 권한과 파일시스템 권한을 함께 점검한다.
접근 로그를 확인하여 비인가 접근 흔적을 조사하고 정기 권한 검토 절차를 운영한다.
```

### 로그 미보관

```text
로그가 생성되지 않거나 보관기간이 부족하면 침해사고 발생 시 원인 분석과 책임추적이 어렵다.
인증, 권한 변경, 관리자 작업, 시스템 오류 로그를 활성화하고 중앙 로그 서버로 전송한다.
시간 동기화를 적용하고 로그 접근권한과 보관기간을 정책에 따라 관리한다.
```

### 랜섬웨어 감염

```text
랜섬웨어 감염이 의심되면 추가 암호화와 내부 확산을 막기 위해 해당 시스템을 네트워크에서 격리한다.
증거 확보 후 악성 프로세스, 자동실행 항목, 외부 통신지를 조사하고 백업을 이용해 복구한다.
재발 방지를 위해 EDR, 메일 보안, 취약점 패치, 최소권한, 오프라인 백업과 복구훈련을 강화한다.
```

## 네트워크보안 답안사례

### 포트 스캔 탐지

```text
동일 출발지에서 짧은 시간 동안 다수의 목적지 포트로 접속 시도가 발생하면 포트 스캔이 의심된다.
방화벽 또는 IPS에서 해당 출발지를 차단하고, 실제 열린 포트와 취약 서비스를 점검한다.
불필요한 외부 노출 포트를 제거하고 스캔 탐지 룰과 임계치를 조정한다.
```

### SYN Flood

```text
SYN 패킷이 급증하고 연결 완료 ACK가 부족하다면 SYN Flood 공격으로 서버의 반쪽 연결 큐가 고갈될 수 있다.
SYN cookie, 연결 제한, rate limit을 적용하고 DDoS 대응장비 또는 상위 ISP와 연계해 트래픽을 완화한다.
공격 지속 여부는 방화벽, 서버, 네트워크 장비 로그의 SYN/ACK 비율로 확인한다.
```

### ARP 스푸핑

```text
게이트웨이 IP에 대응하는 MAC 주소가 비정상적으로 변경되면 ARP 스푸핑으로 중간자 공격이 발생할 수 있다.
공격 의심 단말을 네트워크에서 격리하고 ARP 테이블과 스위치 포트 정보를 확인한다.
재발 방지를 위해 DHCP snooping, Dynamic ARP Inspection, 포트 보안, 정적 ARP를 적용한다.
```

### DNS 영역전송 허용

```text
DNS 영역전송이 외부에서 허용되면 내부 호스트명과 네트워크 구조가 노출되어 공격자가 정찰에 활용할 수 있다.
영역전송은 보조 DNS 서버 등 허가된 IP로만 제한한다.
DNS 서버 설정과 질의 로그를 확인하고 불필요한 레코드 노출을 제거한다.
```

## 어플리케이션보안 답안사례

### SQL 삽입

```text
사용자 입력값이 SQL 문장에 직접 결합되면 공격자가 쿼리 구조를 변경하여 인증 우회, 정보 조회, 데이터 변조를 수행할 수 있다.
Prepared Statement를 사용하고 입력값 검증, 오류 메시지 제한, DB 계정 최소권한을 적용한다.
웹 로그와 DB 감사 로그에서 비정상 쿼리와 대량 조회 여부를 확인한다.
```

### XSS

```text
사용자 입력값이 출력 시 적절히 인코딩되지 않으면 악성 스크립트가 피해자 브라우저에서 실행될 수 있다.
출력 위치에 맞는 문맥별 인코딩을 적용하고 콘텐츠 보안 정책과 HttpOnly 쿠키를 설정한다.
저장형 XSS의 경우 DB에 저장된 악성 스크립트를 제거하고 관련 접근 로그를 확인한다.
```

### CSRF

```text
CSRF 방어가 없으면 공격자가 사용자를 속여 인증된 상태에서 의도하지 않은 요청을 보내게 할 수 있다.
상태 변경 요청에는 CSRF 토큰을 검증하고 SameSite 쿠키, 중요 기능 재인증을 적용한다.
요청 출처와 비정상 요청 로그를 확인하여 피해 범위를 파악한다.
```

### 파일 업로드 웹셸

```text
업로드 파일의 확장자, 내용, 저장 위치, 실행 권한 검증이 미흡하면 웹셸 업로드를 통한 원격 명령 실행이 가능하다.
허용 확장자 방식으로 제한하고 업로드 파일은 웹 루트 밖에 저장하며 실행 권한을 제거한다.
업로드 후 실행 요청 로그를 확인하고 의심 파일을 격리한 뒤 서버 무결성을 점검한다.
```

### 오픈 릴레이

```text
메일 서버가 외부 사용자의 릴레이를 허용하면 스팸 발송지로 악용되어 평판 하락과 차단 목록 등록이 발생할 수 있다.
릴레이는 인증된 사용자 또는 허가된 내부 IP로 제한하고 SMTP 인증과 전송구간 암호화를 적용한다.
메일 큐와 SMTP 로그를 확인해 대량 발송 흔적을 조사한다.
```

## 정보보안일반 답안사례

### 전자서명

```text
전자서명은 메시지의 해시값을 서명자의 개인키로 서명하고 검증자는 서명자의 공개키로 검증한다.
이를 통해 무결성, 서명자 인증, 부인방지를 제공하지만 기밀성은 직접 제공하지 않는다.
문서 내용을 숨기려면 별도의 암호화가 필요하다.
```

### MAC과 전자서명 비교

```text
MAC은 송수신자가 공유한 비밀키로 메시지 인증값을 생성하므로 무결성과 송신자 인증을 제공한다.
그러나 같은 키를 양쪽이 공유하므로 제3자에게 누가 생성했는지 증명하기 어려워 부인방지는 약하다.
부인방지가 필요한 경우 개인키와 공개키 기반의 전자서명을 사용한다.
```

### 인증서 검증

```text
인증서를 검증할 때는 유효기간, 주체 정보, 도메인 일치 여부, 상위 CA 서명, 신뢰 루트까지의 체인, 폐지 여부를 확인해야 한다.
폐지 여부는 CRL 또는 OCSP로 확인한다.
검증을 생략하면 중간자 공격이나 위조 인증서 사용을 탐지하지 못할 수 있다.
```

## 관리 및 법규 답안사례

### 위험평가

```text
위험평가는 보호 대상 자산을 식별하고 해당 자산에 대한 위협과 취약점을 분석한 뒤 가능성과 영향을 기준으로 위험도를 산정하는 절차다.
위험도가 허용 수준을 초과하면 위험감소, 회피, 전가, 수용 중 적절한 처리전략을 선택한다.
대책에는 담당자, 일정, 예산, 검증 방법을 포함하고 조치 후 잔여위험을 승인받아 관리한다.
```

### 개인정보 유출

```text
개인정보 유출이 의심되면 유출 사실, 항목, 규모, 원인, 피해 가능성을 신속히 확인한다.
추가 유출을 차단하고 정보주체 통지, 관계기관 신고, 피해 최소화 조치, 재발 방지 대책을 수행한다.
접근권한, 접속기록, 암호화, 위탁관리 등 안전성 확보조치가 적절했는지 점검한다.
```

### 침해사고 대응

```text
침해사고 대응은 준비, 탐지/분석, 확산 차단, 제거, 복구, 사후개선 순서로 수행한다.
초기 대응에서는 피해 확산을 막되 증거가 훼손되지 않도록 로그와 시스템 상태를 보존한다.
사후에는 원인분석 보고서와 재발 방지 대책을 수립하고 탐지 룰, 접근권한, 보안정책을 개선한다.
```

## 답안에서 피해야 할 표현

| 부족한 답 | 더 좋은 답 |
| --- | --- |
| 보안을 강화한다 | MFA, 접근제어, 로그 모니터링을 적용한다 |
| 암호화한다 | 저장구간 AES 암호화와 키 접근권한 제한을 적용한다 |
| 차단한다 | 방화벽에서 출발지 IP와 공격 패턴을 기준으로 차단한다 |
| 점검한다 | 설정 파일, 로그, 권한 목록을 확인하고 재점검 결과를 기록한다 |
| 관리한다 | 담당자, 주기, 승인 절차, 이력관리를 정의한다 |

