# 01. 시스템보안

## 한눈에 보는 핵심

시스템보안은 단말과 서버, 운영체제, 계정, 권한, 로그, 패치, 악성코드, 취약점 점검, 보안 솔루션을 다룹니다. 시험에서는 `무엇이 위험인지`, `어디를 점검해야 하는지`, `어떻게 조치하는지`를 묻습니다.

## 정보시스템 구성

### 단말 시스템

- 개인용 컴퓨터: 업무 파일, 인증정보, 브라우저 저장정보, 메일 클라이언트, 원격접속 도구가 주요 보호 대상입니다.
- 모바일: 화면잠금, 앱 권한, 저장소 암호화, 탈옥/루팅, 악성 앱, 분실 대응이 중요합니다.
- 프린터/복합기: 주소록, 스캔 문서, 관리자 계정, 웹 관리 페이지, 펌웨어 취약점이 자주 문제됩니다.
- 사물인터넷/산업용 사물인터넷: 기본 계정, 원격관리 포트, 펌웨어 업데이트, 네트워크 분리, 물리적 접근통제가 중요합니다.

### 서버 시스템

- 데이터베이스 서버: 계정 권한, 중요정보 암호화, 감사 로그, 접근통제, 백업 파일 보호가 핵심입니다.
- DNS 서버: 영역전송 제한, 캐시 오염 방지, 재귀 질의 제한, 관리자 접근통제가 중요합니다.
- 메일 서버: 오픈 릴레이 차단, 인증, 악성 첨부 차단, 스팸 대응, 전송구간 암호화가 중요합니다.
- 웹/웹애플리케이션 서버: 취약한 설정, 파일 업로드, 디렉터리 리스팅, 서버 정보 노출, 웹 로그 분석이 자주 나옵니다.
- 파일 서버: 공유 권한, 익명 접근, 중요 파일 암호화, 접근 기록, 랜섬웨어 대응이 중요합니다.
- 클라우드: 계정 권한, 공개 저장소, 보안 그룹, 키 관리, 로그, 백업, 리전/가용성 설계가 핵심입니다.

## 운영체제 보안

### 윈도우 핵심

- 계정 정책: 암호 복잡도, 최소 길이, 최대 사용 기간, 계정 잠금 임계값을 확인합니다.
- 권한 관리: 관리자 권한을 최소화하고, 공유 폴더와 NTFS 권한을 함께 점검합니다.
- 이벤트 로그: 보안 로그, 시스템 로그, 응용프로그램 로그에서 로그인 실패, 권한 상승, 서비스 변경, 계정 생성 이벤트를 확인합니다.
- 서비스 관리: 불필요한 서비스와 원격접속 기능을 비활성화합니다.
- 원격접속: RDP는 접근 허용 대상을 제한하고, 다중인증, 계정 잠금, 접속 로그 확인을 적용합니다.
- 파워셸: 실행 정책, 스크립트 로깅, 의심 명령 실행 기록을 확인합니다.
- 액티브 디렉터리: 도메인 관리자 계정, 그룹 정책, Kerberos 인증, 계정 위임, 서비스 계정 권한을 점검합니다.

### 리눅스/유닉스 핵심

- 계정 파일: `/etc/passwd`, `/etc/shadow`, `/etc/group`에서 불필요 계정, 빈 암호, UID 0 계정을 점검합니다.
- 권한: 소유자, 그룹, 기타 권한과 SUID, SGID, Sticky bit를 이해해야 합니다.
- 특수 권한:
  - SUID: 실행 시 파일 소유자 권한으로 실행됩니다. 불필요한 SUID 파일은 권한 상승 위험입니다.
  - SGID: 실행 시 파일 그룹 권한으로 실행됩니다. 공유 디렉터리에서는 그룹 상속에 쓰입니다.
  - Sticky bit: 공용 디렉터리에서 자기 파일만 삭제하도록 제한합니다.
- `umask`: 새 파일과 디렉터리의 기본 권한을 제한합니다.
- `sudo`: 관리자 명령 허용 범위를 최소화하고 `/etc/sudoers` 문법 오류에 주의합니다.
- SSH: 루트 로그인 금지, 암호 인증 제한, 공개키 인증, 포트 접근제어, 접속 로그 확인이 핵심입니다.
- 로그: `/var/log/auth.log`, `/var/log/secure`, `/var/log/messages`, `/var/log/syslog`, `journalctl`을 봅니다.
- 예약작업: `cron`, `at`에 의심 명령이나 악성 스크립트가 등록되었는지 확인합니다.

## 인증정보와 권한 관리

### 계정 점검

- 사용하지 않는 계정은 잠금 또는 삭제합니다.
- 기본 계정은 이름 변경 또는 비활성화합니다.
- 공유 계정은 책임 추적이 어려우므로 개인별 계정을 사용합니다.
- 관리자 계정은 최소 인원에게만 부여하고 별도 기록을 남깁니다.
- 외주/퇴사/부서이동 계정은 즉시 권한을 회수합니다.

### 암호 정책

- 길이, 복잡도, 재사용 금지, 주기적 변경, 계정 잠금 정책을 조합합니다.
- 관리자 계정과 서비스 계정은 일반 사용자보다 강한 정책을 적용합니다.
- 암호는 평문 저장을 금지하고, 해시와 솔트를 사용합니다.
- 다중인증은 탈취된 암호만으로 접근되는 위험을 줄입니다.

## 시스템 보안위협

### 관리적 위협

- 보안설정 미흡: 기본 계정, 기본 암호, 디렉터리 리스팅, 불필요 서비스
- 권한설정 오류: 과도한 관리자 권한, 전체 공유, 잘못된 파일 권한
- 내부자 위협: 권한 남용, 자료 유출, 로그 삭제, 보안정책 우회
- 패치 미흡: 알려진 취약점이 방치되어 자동화 공격에 노출
- 백업 미흡: 랜섬웨어나 장애 발생 시 복구 불가

### 기술적 위협

- 도청: 평문 프로토콜, 취약한 무선망, 중간자 공격
- 정보유출: 잘못 공개된 저장소, 로그 내 개인정보, 파일 공유 권한 오류
- 정보변조: 웹셸, 권한 탈취, 파일 무결성 훼손
- 비인가 접근: 취약한 원격접속, 암호 대입, 권한 상승
- 악성코드: 랜섬웨어, 트로이목마, 키로거, 백도어, 루트킷

## 시스템 공격기법

### 지능형 지속 공격

흔한 흐름은 `정찰 -> 초기 침투 -> 악성코드 설치 -> 권한 상승 -> 내부 이동 -> 정보 수집 -> 외부 반출 -> 은닉`입니다.

- 초기 침투: 피싱 메일, 악성 첨부, 취약한 공개 서비스
- 권한 상승: 취약한 서비스, 잘못된 파일 권한, 커널 취약점
- 내부 이동: 원격관리 도구, 도메인 계정 탈취, 관리자 공유
- 은닉: 로그 삭제, 정상 프로세스 위장, 예약작업 등록
- 대응: 메일 보안, 패치, 최소권한, EDR, 네트워크 분리, 로그 상관분석

### 버퍼 오버플로

정해진 메모리 영역보다 많은 데이터를 입력하여 인접 메모리를 덮어쓰는 공격입니다. 반환 주소를 조작해 임의 코드를 실행할 수 있습니다.

- 원인: 입력 길이 검증 부족, 안전하지 않은 함수 사용
- 영향: 프로그램 비정상 종료, 권한 상승, 원격 코드 실행
- 대응: 입력값 길이 검증, 안전한 함수, 스택 보호, 주소공간 배치 난수화, 실행 방지, 패치

### SQL 삽입

시스템보안 출제범위에도 포함되어 있으므로 원리와 대응을 알아야 합니다.

- 원인: 사용자 입력값을 SQL 문장에 직접 결합
- 영향: 인증 우회, 데이터 조회/수정/삭제, 관리자 권한 획득
- 대응: 준비된 문장, 입력값 검증, 오류 메시지 숨김, DB 계정 최소권한, 웹방화벽, 로그 점검

### 취약점 스캐너 악용

공격자는 공개 스캐너나 자동화 도구로 열린 포트, 서비스 버전, 취약한 설정을 찾습니다.

- 점검 포인트: 외부 노출 포트, 버전 정보, 기본 페이지, 관리자 콘솔, 취약한 암호화
- 방어 포인트: 서비스 최소화, 접근제어, 배너 숨김, 패치, 탐지 로그 확인

## 시스템 보안 대응기술

### 하드닝

하드닝은 시스템을 안전한 기준에 맞게 강화하는 작업입니다.

- 불필요한 계정 삭제
- 불필요한 서비스 중지
- 기본 암호 변경
- 관리자 접근 경로 제한
- 파일 권한 최소화
- 로그 설정 강화
- 시간 동기화
- 보안패치 적용
- 백업과 복구 절차 점검

### 파일 무결성 점검

중요 파일의 해시값이나 속성 변화를 기록하여 위변조를 탐지합니다.

- 대상: 시스템 실행 파일, 설정 파일, 웹 루트 파일, 계정 파일, 배치/스크립트
- 변화 발생 시 확인: 정상 패치인지, 관리자 변경인지, 침해 흔적인지 구분
- 연계: 변경관리 절차와 로그 기록이 함께 필요합니다.

### 로그 분석

로그는 `누가, 언제, 어디서, 무엇을, 성공/실패 여부` 중심으로 봅니다.

- 인증 로그: 로그인 실패 반복, 성공 직전 다수 실패, 야간 접속
- 권한 로그: 관리자 권한 획득, 계정 생성, 그룹 변경
- 서비스 로그: 서비스 시작/중지, 설정 변경
- 웹 로그: 비정상 경로, 긴 질의문, 업로드 후 실행, 오류 코드 반복
- 보안장비 로그: 차단/탐지 이벤트, 출발지/목적지, 포트, 정책명

### 보안 솔루션

- 백신: 알려진 악성코드 탐지와 치료
- EDR: 단말 행위 기반 탐지, 격리, 조사
- 서버보안: 계정, 권한, 명령어, 파일 접근 통제
- 접근제어: 관리자 접속 승인, 명령어 기록, 세션 녹화
- DLP: 중요정보 유출 탐지와 차단
- 파일무결성 점검: 중요 파일 위변조 탐지
- 취약점 점검 도구: 보안 설정과 취약 버전 식별
- 클라우드 보안 도구: 공개 저장소, 권한, 네트워크 설정, 로그 상태 점검

## 점검 명령어

### 윈도우

| 목적 | 예시 |
| --- | --- |
| 사용자 확인 | `net user` |
| 관리자 그룹 확인 | `net localgroup administrators` |
| 열린 연결 확인 | `netstat -ano` |
| 프로세스 확인 | 작업 관리자, `tasklist` |
| 서비스 확인 | `services.msc`, `sc query` |
| 이벤트 확인 | 이벤트 뷰어 |
| 공유 확인 | `net share` |

### 리눅스

| 목적 | 예시 |
| --- | --- |
| 계정 확인 | `cat /etc/passwd` |
| 로그인 실패 확인 | `lastb`, `/var/log/auth.log` |
| 접속 이력 확인 | `last`, `who`, `w` |
| 열린 포트 확인 | `ss -tulnp`, `netstat -tulnp` |
| 프로세스 확인 | `ps aux`, `top` |
| 권한 상승 파일 확인 | `find / -perm -4000 -type f` |
| 예약작업 확인 | `crontab -l`, `/etc/crontab` |
| 로그 확인 | `journalctl`, `/var/log/*` |

## 실기형 답안 틀

### 취약점 지적

`원인 -> 위험 -> 조치 -> 확인 방법` 순서로 씁니다.

예시: 루트 계정의 원격 로그인이 허용되어 있다면, 공격자가 암호 대입이나 탈취된 암호로 최고권한 접속을 시도할 수 있다. SSH 설정에서 루트 로그인을 금지하고 일반 계정 접속 후 권한 상승 절차를 거치도록 한다. 이후 설정 파일과 인증 로그에서 적용 여부를 확인한다.

### 로그 분석

`이상 징후 -> 공격 추정 -> 근거 로그 -> 차단/복구 조치 -> 재발 방지` 순서로 씁니다.

## 자주 헷갈리는 부분

- 인증과 인가: 인증은 신원 확인, 인가는 권한 부여입니다.
- 파일 권한과 공유 권한: 윈도우 공유에서는 두 권한이 함께 적용되므로 더 제한적인 권한이 실제 권한이 됩니다.
- 탐지와 차단: IDS는 주로 탐지, IPS는 주로 차단까지 수행합니다.
- 백업과 복제: 복제는 장애 대응에 유리하지만 랜섬웨어가 함께 전파될 수 있으므로 오프라인/불변 백업이 필요합니다.
- 패치와 완화: 패치가 원칙이고, 즉시 패치가 어려우면 접근제어, 기능 비활성화, 탐지 강화 같은 임시 완화를 적용합니다.

