# 16. 해설형 문제풀이 훈련

이 파일은 정답만 외우는 문제풀이가 아니라, 왜 그 답이 되는지 사고과정을 설명하는 훈련용입니다. 실제 시험 문제를 그대로 복원한 자료가 아니라, 공식 출제기준 안에서 출제될 법한 형태로 만든 변형 연습문제입니다.

## 필기형 문제

### 1. 계정 관리

문제: 서버에서 퇴사자 계정이 삭제되지 않고 남아 있으며, 해당 계정이 원격접속 권한을 가지고 있다. 가장 적절한 조치는?

정답: 계정을 즉시 잠금 또는 삭제하고, 접속 이력을 확인하며, 계정 회수 절차를 정비한다.

해설: 퇴사자 계정은 더 이상 업무상 필요하지 않으므로 존재 자체가 위험입니다. 단순히 암호만 변경하면 계정 관리 이력이 남지 않거나 재사용될 수 있습니다. 실기 답안에서는 `불필요 계정`, `비인가 접근`, `책임추적`, `접속 로그 확인`, `계정 생명주기 관리`를 함께 써야 합니다.

### 2. SUID

문제: 리눅스 서버에서 일반 사용자가 소유자 권한으로 특정 프로그램을 실행할 수 있는 특수 권한은?

정답: SUID

해설: SUID는 실행 시 파일 소유자의 권한으로 동작합니다. 정상 기능에도 쓰이지만 취약한 SUID 프로그램은 권한 상승으로 이어질 수 있습니다. SGID는 그룹 권한과 관련되고 Sticky bit는 공용 디렉터리에서 삭제 권한을 제한하는 데 사용됩니다.

### 3. 로그 관리

문제: 침해사고 분석을 위해 시스템 로그를 운영할 때 가장 중요한 조합은?

정답: 로그 활성화, 시간 동기화, 중앙 수집, 접근권한 제한, 보관기간 관리

해설: 로그가 있어도 시간이 맞지 않으면 사건 순서를 재구성하기 어렵습니다. 로컬 로그만 있으면 공격자가 삭제할 수 있으므로 중앙 수집이 필요합니다. 로그 자체도 민감한 정보이므로 접근권한을 제한해야 합니다.

### 4. SYN Flood

문제: TCP 연결 요청인 SYN 패킷이 대량 발생하지만 연결 완료 ACK가 거의 없다. 의심되는 공격은?

정답: SYN Flood

해설: SYN Flood는 TCP 3-way handshake의 반쪽 연결 상태를 악용합니다. 대응은 SYN cookie, backlog 조정, rate limit, DDoS 대응장비, 공격 출발지 차단입니다. UDP Flood와 구분하려면 TCP SYN 플래그를 봐야 합니다.

### 5. ARP 스푸핑

문제: 동일 네트워크에서 게이트웨이 IP의 MAC 주소가 갑자기 다른 값으로 바뀌었다. 의심되는 공격과 대응은?

정답: ARP 스푸핑, 공격 단말 격리 및 Dynamic ARP Inspection/포트 보안 적용

해설: ARP는 인증이 없어 공격자가 거짓 ARP 응답을 보낼 수 있습니다. 피해자는 공격자를 게이트웨이로 오인하고 트래픽을 보냅니다. 이 상태에서 스니핑과 패킷 변조가 가능합니다.

### 6. DNS 영역전송

문제: 외부에서 DNS AXFR 질의로 내부 도메인 목록을 가져올 수 있다. 위험과 대응은?

정답: 내부 구조 노출 위험이 있으며, 영역전송을 허가된 보조 DNS 서버 IP로만 제한한다.

해설: 영역전송은 주 DNS와 보조 DNS 사이의 동기화 기능입니다. 외부에 열려 있으면 서버명, 내부망 구조, 서비스명을 공격자에게 알려주는 정찰 자료가 됩니다.

### 7. SQL 삽입

문제: 로그인 기능에서 사용자 입력값을 SQL 문자열에 직접 붙이고 있다. 핵심 대응은?

정답: Prepared Statement 사용

해설: Prepared Statement는 SQL 코드와 데이터를 분리합니다. 입력값 검증도 필요하지만 핵심 대응은 쿼리 구조가 바뀌지 않도록 하는 것입니다. DB 계정 최소권한과 오류 메시지 제한은 보조 대책입니다.

### 8. XSS와 CSRF

문제: XSS와 CSRF의 가장 중요한 차이는?

정답: XSS는 악성 스크립트가 피해자 브라우저에서 실행되는 공격이고, CSRF는 인증된 사용자의 권한으로 원치 않는 요청을 보내게 하는 공격이다.

해설: XSS의 핵심 대응은 출력 인코딩입니다. CSRF의 핵심 대응은 CSRF 토큰과 SameSite 쿠키입니다. 둘 다 웹 취약점이지만 원인과 대응이 다릅니다.

### 9. 파일 업로드

문제: 파일 업로드 기능에서 `.jsp` 파일이 웹 루트에 저장되고 실행된다. 발생 가능한 위험은?

정답: 웹셸을 통한 원격 명령 실행

해설: 실행 가능한 파일이 웹 루트에 저장되고 실행 권한까지 있으면 공격자는 서버 명령을 실행할 수 있습니다. 대응은 허용 확장자, 저장 위치 분리, 실행 권한 제거, 파일명 난수화, 내용 검사입니다.

### 10. 오픈 릴레이

문제: 메일 서버가 외부 사용자의 메일을 제3자 도메인으로 릴레이한다. 위험은?

정답: 스팸 발송에 악용되어 도메인 평판 하락과 차단 목록 등록이 발생할 수 있다.

해설: 메일 서버는 허가된 사용자나 내부 IP만 릴레이해야 합니다. SPF, DKIM, DMARC는 발신자 위조 대응에 도움이 되지만 오픈 릴레이 자체는 릴레이 설정으로 제한해야 합니다.

### 11. 해시

문제: 해시함수에 대한 설명으로 옳은 것은?

정답: 임의 길이 입력을 고정 길이 출력으로 바꾸며, 일반적으로 복호화하지 않는다.

해설: 해시는 암호화가 아닙니다. 복호화가 목적이 아니라 무결성 검증과 요약입니다. 비밀번호 저장에는 단순 해시가 아니라 Salt와 느린 KDF가 필요합니다.

### 12. MAC

문제: MAC이 전자서명보다 부인방지에 약한 이유는?

정답: 송신자와 수신자가 같은 비밀키를 공유하기 때문이다.

해설: 같은 키를 가진 수신자도 MAC을 만들 수 있으므로 제3자에게 누가 만들었는지 강하게 증명하기 어렵습니다. 전자서명은 개인키 소유자만 서명할 수 있어 부인방지를 제공합니다.

### 13. 공개키 암호와 전자서명

문제: 기밀성을 위해 공개키 암호를 사용할 때 어떤 키로 암호화하는가?

정답: 수신자의 공개키

해설: 수신자의 공개키로 암호화하면 수신자의 개인키로만 복호화할 수 있습니다. 전자서명은 반대로 송신자의 개인키로 서명하고 송신자의 공개키로 검증합니다.

### 14. Diffie-Hellman

문제: Diffie-Hellman 키 교환을 단독으로 사용할 때 취약한 공격은?

정답: 중간자 공격

해설: Diffie-Hellman은 공통 비밀키를 만들 수 있지만 상대방 인증을 직접 제공하지 않습니다. 공격자가 중간에서 공개 값을 바꿔치기할 수 있으므로 인증서나 전자서명과 함께 사용해야 합니다.

### 15. 접근통제

문제: 직무 역할에 따라 권한을 부여하는 접근통제 모델은?

정답: RBAC

해설: RBAC는 역할 기반 접근통제입니다. DAC는 소유자 중심, MAC은 보안 등급 중심, ABAC는 속성 중심입니다.

### 16. Bell-LaPadula

문제: Bell-LaPadula 모델이 중점적으로 보호하는 보안 속성은?

정답: 기밀성

해설: Bell-LaPadula는 기밀성 모델입니다. Biba는 무결성 모델입니다. 시험에서는 둘을 자주 바꿔서 냅니다.

### 17. 위험관리

문제: 패치 미흡은 자산, 위협, 취약점, 위험 중 무엇인가?

정답: 취약점

해설: 자산은 서버나 데이터 같은 보호 대상입니다. 위협은 해킹, 악성코드, 내부자 같은 원인입니다. 취약점은 위협이 이용할 수 있는 약점입니다. 위험은 위협이 취약점을 이용해 손실을 만들 가능성과 영향입니다.

### 18. 위험처리

문제: 취약한 서비스를 폐쇄하여 해당 위험이 발생하지 않게 하는 전략은?

정답: 위험회피

해설: 위험감소는 보호대책으로 위험도를 낮추는 것입니다. 위험전가는 보험이나 계약으로 손실 부담을 넘기는 것입니다. 위험수용은 잔여위험을 승인하는 것입니다.

### 19. 개인정보 안전성 확보조치

문제: 개인정보처리자가 개인정보 보호를 위해 적용해야 하는 안전성 확보조치의 예는?

정답: 접근권한 관리, 접근통제, 암호화, 접속기록 보관, 악성프로그램 방지, 물리적 보호조치

해설: 개인정보보호 문제는 기술적 조치만 묻지 않습니다. 내부관리계획, 취급자 교육, 위탁관리 같은 관리적 조치도 함께 생각해야 합니다.

### 20. 침해사고 대응

문제: 침해사고 대응 절차의 올바른 흐름은?

정답: 준비 -> 탐지/분석 -> 확산 차단 -> 제거 -> 복구 -> 사후개선

해설: 사고 대응에서 무작정 삭제하거나 재부팅하는 것은 증거 훼손 위험이 있습니다. 확산 차단과 증거 보존을 함께 고려해야 합니다.

## 실기형 문제

### 1. SSH root 로그인 허용

문제: 리눅스 서버에서 SSH root 로그인이 허용되어 있다. 위험과 조치를 쓰시오.

모범답안:

```text
root 원격 로그인이 허용되어 있으면 공격자가 암호 대입이나 탈취된 인증정보로 최고권한을 직접 획득할 수 있다.
SSH 설정에서 root 직접 로그인을 금지하고 일반 계정으로 접속한 뒤 sudo를 통해 필요한 명령만 수행하도록 한다.
접속 허용 IP 제한, 공개키 인증, MFA를 적용하고 인증 로그에서 비정상 접속 여부를 확인한다.
```

채점 포인트: root 직접 로그인 위험, 최고권한, 설정 변경, 접근 제한, 로그 확인이 들어가야 합니다.

### 2. 웹 로그에 `' OR '1'='1`

문제: 웹 접근 로그에 `' OR '1'='1` 문자열이 반복된다. 공격명과 대응을 쓰시오.

모범답안:

```text
SQL 삽입 공격 시도가 의심된다. 사용자 입력값이 SQL 문장에 직접 결합되면 인증 우회나 데이터 조회, 변조가 가능하다.
Prepared Statement를 적용하고 입력값 검증, 오류 메시지 제한, DB 계정 최소권한을 적용한다.
웹 로그와 DB 감사 로그를 확인하여 공격 성공 여부와 대량 조회 여부를 조사한다.
```

채점 포인트: SQL 삽입, 원인, 영향, Prepared Statement, DB 로그 확인.

### 3. DNS AXFR 성공

문제: 외부 PC에서 DNS 영역전송이 성공했다. 보안상 문제와 조치를 쓰시오.

모범답안:

```text
DNS 영역전송이 외부에 허용되면 내부 호스트명과 네트워크 구조가 노출되어 공격자의 정찰 자료로 활용될 수 있다.
영역전송은 허가된 보조 DNS 서버 IP로만 제한하고 불필요한 레코드는 제거한다.
DNS 질의 로그를 확인하여 외부에서 영역전송이 수행된 이력을 조사한다.
```

### 4. 개인정보 DB 평문 저장

문제: 고객 DB에 주민등록번호가 평문으로 저장되어 있다. 위험과 보호대책을 쓰시오.

모범답안:

```text
고유식별정보가 평문으로 저장되어 있으면 DB 유출 시 직접적인 개인정보 침해와 법적 책임이 발생할 수 있다.
저장 구간 암호화, DB 접근권한 최소화, 접속기록 보관과 점검, 관리자 접근통제를 적용한다.
암호화 키는 별도 보관하고 접근권한을 제한하며, 백업 파일도 동일하게 암호화한다.
```

### 5. 방화벽 Any Any Allow

문제: 방화벽에 `Any -> Any -> Allow` 정책이 존재한다. 문제점과 개선방안을 쓰시오.

모범답안:

```text
Any Any Allow 정책은 모든 출발지와 목적지, 서비스 접근을 허용하므로 비인가 접근과 공격 트래픽 통제가 불가능하다.
업무상 필요한 출발지, 목적지, 포트만 허용하도록 최소허용 정책으로 변경한다.
정책별 승인 근거, 담당자, 만료일, 로그 설정을 관리하고 불필요한 임시 정책은 제거한다.
```

### 6. 랜섬웨어 의심

문제: 파일 확장자가 일괄 변경되고 파일이 열리지 않는다. 대응 절차를 쓰시오.

모범답안:

```text
랜섬웨어 감염이 의심되므로 추가 확산을 막기 위해 해당 시스템을 네트워크에서 격리한다.
증거 보존 후 프로세스, 자동실행 항목, 네트워크 연결, 로그를 수집하고 감염 원인을 분석한다.
악성코드 제거와 취약점 조치 후 안전한 백업으로 복구하고, EDR, 메일 보안, 최소권한, 오프라인 백업을 강화한다.
```

### 7. 인증서 오류 무시

문제: 사용자가 웹 접속 시 인증서 경고를 무시하도록 안내받고 있다. 위험과 대책을 쓰시오.

모범답안:

```text
인증서 경고를 무시하면 위조 서버나 중간자 공격을 탐지하지 못해 인증정보와 개인정보가 노출될 수 있다.
인증서의 유효기간, 도메인, 인증서 체인, 폐지 여부를 정상 검증해야 한다.
서버 인증서를 신뢰 가능한 CA에서 재발급하거나 설정 오류를 수정하고, 사용자가 인증서 오류를 우회하지 않도록 정책과 교육을 적용한다.
```

### 8. 퇴사자 계정 접속 성공

문제: 퇴사자 계정으로 VPN 접속이 성공한 로그가 발견되었다. 대응을 쓰시오.

모범답안:

```text
퇴사자 계정은 더 이상 업무상 필요하지 않으므로 비인가 접근에 해당할 수 있다.
해당 계정을 즉시 잠금 처리하고 VPN 세션을 종료하며 접속 시간, 출발지 IP, 접근 시스템, 수행 행위를 조사한다.
퇴사 및 권한 변경 절차에서 계정 회수, MFA 토큰 회수, 정기 권한 검토가 수행되도록 개선한다.
```

## 문제풀이 요령

필기에서 모르는 문제가 나오면 다음 기준으로 좁힙니다.

1. `항상`, `완전히`, `절대` 같은 표현은 의심합니다.
2. 공격의 원인과 대응이 맞는 짝인지 확인합니다.
3. 보안 속성을 구분합니다. 기밀성, 무결성, 인증, 부인방지는 서로 다릅니다.
4. 관리 문제는 절차와 책임을 봅니다.
5. 법규 문제는 최신 법령 확인이 필요하므로 숫자와 시한은 시험 직전에 다시 봅니다.

실기에서 모르는 문제가 나오면 빈칸을 만들지 말고 다음 구조로 씁니다.

```text
이 문제는 [취약한 설정/공격/위험]과 관련된다.
해당 상태에서는 [기밀성/무결성/가용성/책임추적/법적 의무] 문제가 발생할 수 있다.
따라서 [접근통제/암호화/패치/로그/백업/교육/절차]를 적용하고, [로그/설정/재점검]으로 확인한다.
```

