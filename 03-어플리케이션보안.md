# 03. 어플리케이션보안

## 한눈에 보는 핵심

어플리케이션보안은 인터넷 서비스의 구조와 취약점을 묻습니다. FTP, 메일, 웹, DNS, DB는 `서비스 동작 -> 대표 공격 -> 보안 설정 -> 로그 확인` 순서로 정리하면 필기와 실기에 모두 연결됩니다.

## FTP 보안

### 구조

- FTP는 파일 전송 서비스입니다.
- 제어 연결과 데이터 연결을 분리합니다.
- 능동 모드와 수동 모드가 있으며 방화벽 정책과 관련됩니다.
- 기본 FTP는 인증정보와 데이터가 평문으로 전달될 수 있습니다.

### 주요 위험

- 익명 접속 허용
- 평문 계정 전송
- 약한 암호와 암호 대입
- 과도한 업로드 권한
- 디렉터리 이동 제한 미흡
- FTP Bounce 공격
- 오래된 서버 버전 취약점

### 대응

- 익명 접속 금지 또는 격리
- SFTP 또는 FTPS 사용
- 계정별 디렉터리 제한
- 업로드 파일 실행 권한 제거
- 접근 허용 IP 제한
- 로그인 실패 제한
- 서버 배너 최소화
- 로그 확인

## 메일 보안

### 구조

- SMTP: 메일 전송
- POP3: 메일 내려받기
- IMAP: 서버에 메일을 보관하며 동기화
- MTA: 메일 전송 서버
- MUA: 사용자 메일 클라이언트

### 공격 유형

- 스팸 메일
- 피싱 메일
- 악성 첨부
- 악성 링크
- 발신자 위조
- 오픈 릴레이 악용
- 메일 계정 탈취
- 웜 전파

### 대응

- 오픈 릴레이 차단
- 발신 인증과 강한 암호
- 다중인증
- SPF, DKIM, DMARC 적용
- 첨부파일 악성코드 검사
- URL 검사
- 스팸 필터링
- 전송구간 암호화
- 메일 로그 모니터링

### SPF, DKIM, DMARC

- SPF: 발신 도메인의 허용 메일 서버를 DNS에 등록해 발신 IP를 검증합니다.
- DKIM: 메일 헤더에 전자서명을 붙여 위변조 여부와 도메인 신뢰성을 확인합니다.
- DMARC: SPF와 DKIM 검증 실패 시 처리 정책을 선언하고 보고서를 받습니다.

## 웹/앱 보안

### 웹 서비스 기본

- HTTP는 요청과 응답 구조입니다.
- HTTPS는 TLS를 통해 전송구간을 보호합니다.
- 쿠키와 세션은 인증 상태 유지에 사용됩니다.
- 웹 서버, 웹애플리케이션 서버, 데이터베이스 서버가 분리되어 운영되는 경우가 많습니다.

### 주요 웹 취약점

#### SQL 삽입

- 원인: 입력값을 SQL에 직접 결합
- 영향: 인증 우회, 정보 조회, 데이터 변조, 시스템 명령 실행
- 대응: 준비된 문장, 입력값 검증, DB 권한 최소화, 오류 메시지 제한

#### 크로스 사이트 스크립팅

- 원인: 사용자 입력값을 검증/인코딩 없이 페이지에 출력
- 영향: 세션 탈취, 악성 스크립트 실행, 피싱
- 대응: 출력 인코딩, 입력값 검증, 콘텐츠 보안 정책, 쿠키 보안 속성

#### 크로스 사이트 요청 위조

- 원인: 사용자가 인증된 상태라는 점을 악용해 의도하지 않은 요청을 보내게 함
- 영향: 비밀번호 변경, 결제, 글쓰기 등 권한 있는 기능 오남용
- 대응: CSRF 토큰, SameSite 쿠키, 중요 기능 재인증

#### 파일 업로드 취약점

- 원인: 확장자, MIME, 저장 위치, 실행 권한 검증 미흡
- 영향: 웹셸 업로드, 원격 명령 실행
- 대응: 허용 확장자 방식, 저장 경로 분리, 실행 권한 제거, 파일명 난수화, 내용 검사

#### 경로 조작

- 원인: `../` 같은 경로 입력을 제한하지 않음
- 영향: 시스템 파일 읽기, 설정 파일 노출
- 대응: 경로 정규화, 허용 목록, 웹 루트 외부 접근 차단

#### 접근통제 오류

- 원인: 권한 검사를 화면이나 메뉴 노출에만 의존
- 영향: 다른 사용자의 정보 조회/수정
- 대응: 서버 측 권한 검사, 객체 소유자 검증, 권한 매트릭스 테스트

#### 서버 측 요청 위조

- 원인: 서버가 사용자 입력 URL로 내부 자원에 요청
- 영향: 내부망 스캔, 메타데이터 탈취, 내부 서비스 접근
- 대응: 내부 주소 차단, 허용 도메인 목록, 리다이렉트 제한, 응답 제한

### 웹 서버 설정 점검

- 디렉터리 리스팅 비활성화
- 서버 버전 정보 노출 최소화
- 기본 페이지와 샘플 파일 제거
- 관리자 페이지 접근 제한
- 업로드 디렉터리 실행 권한 제거
- TLS 설정 강화
- 보안 헤더 적용
- 로그 저장과 보관

### 웹 로그 분석

| 로그 특징 | 의심 행위 |
| --- | --- |
| `' or '1'='1` 같은 문자열 | SQL 삽입 |
| `<script>` 문자열 | 스크립트 삽입 |
| `../` 반복 | 경로 조작 |
| `.jsp`, `.php` 업로드 후 접근 | 웹셸 |
| 관리자 경로 반복 접근 | 무차별 대입 또는 스캔 |
| 비정상 User-Agent | 자동화 도구 |

## DNS 보안

### 구조

- 도메인 이름을 IP 주소로 변환합니다.
- 재귀 질의와 반복 질의가 있습니다.
- 주요 레코드: A, AAAA, CNAME, MX, NS, TXT, PTR

### 공격 유형

- DNS 캐시 오염
- 영역전송 정보노출
- DNS 증폭 공격
- 도메인 탈취
- DNS 터널링
- 악성 도메인 질의

### 대응

- 영역전송 허용 대상을 제한합니다.
- 불필요한 재귀 질의를 차단합니다.
- DNSSEC을 적용합니다.
- 관리자 계정과 등록기관 계정을 보호합니다.
- DNS 로그를 모니터링합니다.
- 내부 DNS와 외부 DNS를 분리합니다.

## DB 보안

### 주요 위험

- 기본 계정과 약한 암호
- 과도한 DBA 권한
- 중요정보 평문 저장
- 감사 로그 미설정
- SQL 삽입
- 백업 파일 노출
- 개발/운영 DB 분리 미흡

### 대응

- 계정별 최소권한
- 중요정보 암호화
- 접근제어와 승인 절차
- 감사 로그 활성화
- DB 방화벽 또는 접근제어 솔루션
- 백업 파일 암호화와 접근통제
- 개발 데이터 비식별 처리
- 패치와 취약점 점검

### 중요정보 보호

- 저장 시 암호화: 주민등록번호, 비밀번호, 결제정보 등
- 비밀번호: 복호화 가능한 암호화가 아니라 안전한 해시와 솔트 사용
- 전송 시 암호화: DB 접속 구간 TLS 적용
- 마스킹: 조회 화면과 테스트 데이터에서 노출 최소화

## 전자상거래 보안

### 핵심 보안요소

- 거래 당사자 인증
- 거래 내용 무결성
- 결제정보 기밀성
- 부인방지
- 개인정보 보호
- 이상거래 탐지

### 전자지불 수단별 고려사항

- 카드 결제: 카드정보 보호, 가맹점 서버 저장 금지, 결제대행 연동 보안
- 계좌이체: 사용자 인증, 위변조 방지, 거래내역 확인
- 간편결제: 토큰화, 기기 인증, 생체 인증, 이상거래 탐지
- 모바일 결제: 앱 위변조 방지, 루팅/탈옥 탐지, 안전한 저장소 사용

## 개발보안

### 기본 원칙

- 입력은 신뢰하지 않습니다.
- 출력은 맥락에 맞게 인코딩합니다.
- 인증과 권한 검사는 서버에서 수행합니다.
- 오류 메시지에 내부 정보를 노출하지 않습니다.
- 비밀값은 코드에 넣지 않습니다.
- 로그에는 비밀번호와 민감정보를 남기지 않습니다.
- 외부 라이브러리 취약점을 관리합니다.

### 안전한 코딩 핵심

| 위험 | 대응 |
| --- | --- |
| SQL 삽입 | 준비된 문장 |
| 스크립트 삽입 | 출력 인코딩 |
| 파일 업로드 | 확장자 허용 목록, 실행권한 제거 |
| 인증 우회 | 서버 측 인증/인가 |
| 세션 탈취 | Secure, HttpOnly, SameSite |
| 경로 조작 | 경로 정규화와 허용 목록 |
| 명령 삽입 | 운영체제 명령 호출 최소화, 인자 검증 |
| 비밀값 노출 | 비밀관리 도구와 환경 분리 |

### 개발보안 도구

- 정적 분석: 소스코드에서 취약한 패턴을 찾습니다.
- 동적 분석: 실행 중인 애플리케이션을 대상으로 취약점을 찾습니다.
- 구성요소 분석: 오픈소스 라이브러리 취약점과 라이선스를 확인합니다.
- 비밀값 탐지: 코드 저장소의 API 키, 암호, 토큰을 찾습니다.

## 장애 분석과 보안 사고 구분

웹/앱 문제는 단순 장애인지 공격인지 구분해야 합니다.

- 단순 장애: 특정 배포 이후 오류 증가, 내부 설정 오류, 자원 부족
- 공격 의심: 외부 특정 IP 요청 급증, 비정상 경로 반복, 인증 실패 증가, 웹셸 파일 생성
- 확인 자료: 애플리케이션 로그, 웹 서버 접근 로그, 오류 로그, DB 로그, 방화벽 로그, 변경 이력

## 실기 답안 틀

어플리케이션 문제는 다음 흐름으로 씁니다.

1. 취약한 입력/설정/권한을 지적합니다.
2. 공격자가 얻을 수 있는 결과를 설명합니다.
3. 코드 또는 설정 수준의 조치를 제시합니다.
4. 로그와 재점검 방법을 적습니다.

예시: 파일 업로드 기능에서 실행 가능한 확장자를 차단하지 않으면 공격자가 웹셸을 업로드해 서버 명령을 실행할 수 있다. 업로드는 허용 확장자 방식으로 제한하고 저장 위치를 웹 루트 밖으로 분리하며 실행 권한을 제거한다. 업로드 로그와 웹 접근 로그에서 업로드 파일 실행 시도를 확인하고, 배포 후 재점검한다.

