# 21. 실기 로그·패킷 분석 워크북

정보보안기사 실기는 `로그나 패킷에서 근거를 찾고, 공격명과 대응을 문장으로 쓰는 능력`이 중요합니다. 이 문서는 실제 로그처럼 보이는 단서를 보고 답안을 구성하는 훈련용입니다.

## 로그 분석 기본 순서

```mermaid
flowchart LR
  A["시간"] --> B["출발지/계정"]
  B --> C["목적지/서비스"]
  C --> D["행위/상태코드"]
  D --> E["반복성/이상패턴"]
  E --> F["공격명 추정"]
  F --> G["조치와 재발방지"]
```

로그 분석 답안은 보통 다음 순서로 쓰면 안정적입니다.

```text
로그에서 [관찰 근거]가 확인되므로 [공격명/취약점]이 의심된다.
해당 공격은 [원리]로 인해 [위험]을 초래할 수 있다.
즉시 [차단/격리/계정조치/증거보존]을 수행하고,
이후 [패치/설정변경/권한정리/탐지정책/모니터링]으로 재발을 방지한다.
```

## 자주 보는 로그 필드

| 필드 | 의미 | 시험에서 보는 포인트 |
| --- | --- | --- |
| 시간 | 이벤트 발생 시각 | 짧은 시간 반복, 야간/휴일 접근, 여러 장비 시간순서 |
| 출발지 IP | 요청을 보낸 주소 | 단일 공격원, 분산 공격원, 내부자 여부 |
| 목적지 IP/포트 | 접근 대상 | 노출 서비스, 불필요 포트, 공격 대상 |
| 계정 | 인증 주체 | 관리자 계정, 퇴직자 계정, 실패 후 성공 |
| 메서드/URI | 웹 요청 행위 | SQLi, XSS, 경로조작, 웹셸 실행 |
| 상태코드 | 처리 결과 | 200 성공, 30x 리다이렉트, 401/403 인증/인가 실패, 404 탐색, 500 오류 |
| User-Agent | 클라이언트 정보 | 자동화 도구, 스캐너, 비정상 문자열 |
| TCP Flags | TCP 제어 비트 | SYN Flood, FIN/Xmas scan, 비정상 플래그 |
| 탐지명 | 보안장비 이벤트 | IDS/IPS/WAF 룰명과 실제 서버 로그 상관분석 |

## 케이스 1. SSH 무차별 대입 후 성공

### 로그

```text
May 03 01:12:05 web01 sshd[1881]: Failed password for invalid user admin from 203.0.113.45 port 51120 ssh2
May 03 01:12:08 web01 sshd[1882]: Failed password for root from 203.0.113.45 port 51121 ssh2
May 03 01:12:11 web01 sshd[1883]: Failed password for test from 203.0.113.45 port 51122 ssh2
May 03 01:14:02 web01 sshd[1901]: Accepted password for deploy from 203.0.113.45 port 51230 ssh2
May 03 01:15:10 web01 sudo: deploy : TTY=pts/1 ; PWD=/home/deploy ; USER=root ; COMMAND=/bin/bash
```

### 판단

- 같은 출발지에서 여러 계정으로 로그인 실패가 반복됩니다.
- 이후 `deploy` 계정 로그인 성공과 `sudo`를 통한 root 권한 획득이 확인됩니다.
- SSH 무차별 대입 또는 크리덴셜 스터핑 후 권한 상승이 의심됩니다.

### 답안 예시

```text
동일 IP에서 여러 계정에 대한 SSH 로그인 실패가 반복된 뒤 deploy 계정으로 로그인 성공하고 sudo를 통해 root 권한을 획득한 로그가 확인되므로 SSH 무차별 대입 또는 탈취 계정 사용이 의심된다.
즉시 해당 출발지 IP를 차단하고 deploy 계정의 세션 종료, 비밀번호 변경, 권한 검토를 수행한다.
인증 로그와 명령 실행 이력을 보존하여 추가 침해 여부를 확인하고, root 직접 로그인 금지, 공개키 인증, MFA, 접속 허용 IP 제한, 계정 잠금 정책을 적용한다.
```

## 케이스 2. 웹 경로조작

### 로그

```text
203.0.113.10 - - [03/May/2026:10:05:11 +0900] "GET /download?file=../../../../etc/passwd HTTP/1.1" 200 1842
203.0.113.10 - - [03/May/2026:10:05:13 +0900] "GET /download?file=..%2f..%2f..%2f..%2fetc%2fshadow HTTP/1.1" 403 532
```

### 판단

- `../`와 URL 인코딩된 `..%2f`가 보입니다.
- 다운로드 기능에서 허용 경로 밖의 시스템 파일 접근을 시도합니다.
- 첫 요청이 200이면 일부 파일 노출 가능성을 확인해야 합니다.

### 답안 예시

```text
다운로드 파라미터에 ../ 및 URL 인코딩된 상위 경로 이동 문자열이 포함되어 있어 경로조작 공격이 의심된다.
공격자는 허용된 디렉터리 밖의 시스템 파일이나 설정 파일을 열람할 수 있으므로 해당 기능을 임시 차단하고 접근 로그와 응답 크기를 확인하여 정보 노출 여부를 조사한다.
파일명은 허용목록 기반으로 처리하고, 경로 정규화 후 기준 디렉터리 밖 접근을 차단하며, 다운로드 파일은 식별자 기반으로 매핑한다.
```

## 케이스 3. SQL Injection 탐색

### 로그

```text
198.51.100.77 - - [03/May/2026:11:21:03 +0900] "GET /login?id=admin'--&pw=x HTTP/1.1" 500 912
198.51.100.77 - - [03/May/2026:11:21:05 +0900] "GET /search?q=' UNION SELECT id,pw FROM member-- HTTP/1.1" 200 5234
198.51.100.77 - - [03/May/2026:11:21:08 +0900] "GET /item?id=1 OR 1=1 HTTP/1.1" 200 8840
```

### 판단

- 작은따옴표, 주석, `UNION SELECT`, `OR 1=1`이 보입니다.
- 500 오류는 쿼리 문법 오류나 DB 오류 노출 가능성을 뜻합니다.
- 200과 응답 크기 증가는 공격이 일부 성공했을 가능성을 시사합니다.

### 답안 예시

```text
요청 파라미터에 SQL 주석, UNION SELECT, OR 1=1 조건이 포함되어 있어 SQL Injection 시도가 확인된다.
입력값이 SQL 문으로 해석되면 인증 우회, 개인정보 조회, 데이터 변조가 가능하므로 해당 요청을 차단하고 웹 로그와 DB 감사 로그를 상관분석하여 실제 조회 여부를 확인한다.
Prepared Statement를 적용하고 DB 계정 최소권한, 오류 메시지 비노출, WAF 탐지정책, 취약 파라미터 재점검을 수행한다.
```

## 케이스 4. XSS 시도

### 로그

```text
192.0.2.15 - - [03/May/2026:12:02:44 +0900] "GET /board/write?title=<script>alert(1)</script> HTTP/1.1" 200 1240
192.0.2.15 - - [03/May/2026:12:03:01 +0900] "GET /profile?name=%3Cimg%20src=x%20onerror=fetch('/me')%3E HTTP/1.1" 200 1444
```

### 판단

- `<script>`, `onerror` 이벤트 핸들러가 보입니다.
- 응답이 200이면 입력값이 저장 또는 반사되는지 확인해야 합니다.

### 답안 예시

```text
요청 파라미터에 script 태그와 이벤트 핸들러가 포함되어 있어 XSS 시도가 의심된다.
입력값이 화면에 그대로 출력되면 피해자 브라우저에서 악성 스크립트가 실행되어 세션 탈취나 피싱으로 이어질 수 있다.
출력 위치에 맞는 문맥별 인코딩을 적용하고, 저장형 여부를 확인하여 DB에 저장된 악성 스크립트를 제거하며, CSP와 HttpOnly/Secure/SameSite 쿠키 설정을 적용한다.
```

## 케이스 5. 웹셸 업로드와 실행

### 로그

```text
203.0.113.88 - - [03/May/2026:13:10:22 +0900] "POST /upload HTTP/1.1" 200 98
203.0.113.88 - - [03/May/2026:13:10:31 +0900] "GET /uploads/profile.php?cmd=id HTTP/1.1" 200 81
203.0.113.88 - - [03/May/2026:13:10:35 +0900] "GET /uploads/profile.php?cmd=cat+/etc/passwd HTTP/1.1" 200 2210
```

### 판단

- 업로드 직후 업로드 경로의 `.php` 파일이 실행됩니다.
- `cmd=id`, `cat /etc/passwd`는 웹셸 명령 실행 단서입니다.

### 답안 예시

```text
업로드 직후 업로드 디렉터리의 PHP 파일에 cmd 파라미터로 OS 명령이 전달되어 웹셸 업로드와 원격 명령 실행이 의심된다.
즉시 해당 파일을 격리하고 웹서버 프로세스 권한으로 실행된 명령, 추가 파일 생성, 외부 통신 여부를 조사한다.
업로드 파일은 허용 확장자와 파일 시그니처를 검증하고 웹 루트 밖에 저장하며, 업로드 경로의 스크립트 실행 권한을 제거하고 서버 무결성 점검과 계정 권한 검토를 수행한다.
```

## 케이스 6. SYN Flood 패킷 단서

### 패킷 요약

```text
10:01:01.100 198.51.100.1:45111 -> 10.0.0.10:80 SYN
10:01:01.101 198.51.100.2:45112 -> 10.0.0.10:80 SYN
10:01:01.102 198.51.100.3:45113 -> 10.0.0.10:80 SYN
10:01:01.103 198.51.100.4:45114 -> 10.0.0.10:80 SYN
10:01:01.104 198.51.100.5:45115 -> 10.0.0.10:80 SYN
```

### 판단

- 짧은 시간에 다수 출발지에서 동일 목적지 80번 포트로 SYN이 집중됩니다.
- ACK가 뒤따르지 않으면 반쪽 연결 자원 고갈 가능성이 큽니다.

### 답안 예시

```text
동일 웹 서버로 SYN 패킷이 대량 유입되고 연결 완료 ACK가 부족하다면 SYN Flood 공격이 의심된다.
서버의 연결 대기 큐가 고갈되어 정상 사용자의 접속 지연이나 서비스 거부가 발생할 수 있다.
SYN Cookie, 연결 제한, rate limit, 방화벽/IPS 차단, DDoS 대응장비 또는 상위 ISP 연계를 적용하고, 공격 종료 후 임계치와 모니터링 정책을 조정한다.
```

## 케이스 7. ARP Spoofing

### 단서

```text
정상 시:
Gateway 10.0.0.1 -> aa:aa:aa:aa:aa:aa

이상 시:
Gateway 10.0.0.1 -> bb:bb:bb:bb:bb:bb
Attacker 10.0.0.55 -> bb:bb:bb:bb:bb:bb
```

### 판단

- 게이트웨이 IP에 대응하는 MAC 주소가 공격 의심 단말의 MAC과 같아졌습니다.
- 피해자의 트래픽이 공격자를 경유할 수 있습니다.

### 답안 예시

```text
게이트웨이 IP의 MAC 주소가 공격 의심 단말의 MAC으로 변경되어 ARP Spoofing이 의심된다.
공격자는 중간자 위치에서 트래픽을 도청하거나 변조할 수 있으므로 의심 단말을 격리하고 피해 단말의 ARP 캐시와 스위치 포트 정보를 확인한다.
DHCP Snooping, Dynamic ARP Inspection, 포트 보안, 중요 구간 정적 ARP, 암호화 통신을 적용한다.
```

## 케이스 8. DNS 오픈 리졸버와 증폭

### 로그

```text
May 03 14:10:00 dns01 named[330]: client 198.51.100.90#53000: query: example.net ANY +E (10.0.0.53)
May 03 14:10:00 dns01 named[330]: client 198.51.100.91#53001: query: example.net ANY +E (10.0.0.53)
May 03 14:10:01 dns01 named[330]: client 198.51.100.92#53002: query: example.net ANY +E (10.0.0.53)
```

### 판단

- 외부 클라이언트가 `ANY` 질의를 반복합니다.
- 재귀 질의가 외부에 열려 있으면 DNS 증폭 공격에 악용될 수 있습니다.

### 답안 예시

```text
외부 IP에서 DNS ANY 질의가 반복되어 오픈 리졸버 악용 또는 DNS 증폭 공격 참여가 의심된다.
DNS 서버가 공격 반사 지점으로 사용되면 제3자 피해와 서비스 평판 하락이 발생할 수 있다.
재귀 질의는 내부 허용 대역으로 제한하고, ANY 응답 제한, rate limit, 불필요한 외부 질의 차단, DNS 로그 모니터링을 적용한다.
```

## 케이스 9. 메일 오픈 릴레이

### 로그

```text
May 03 15:22:10 mail01 postfix/smtpd[901]: connect from unknown[203.0.113.200]
May 03 15:22:11 mail01 postfix/smtpd[901]: NOQUEUE: relay: RCPT from unknown[203.0.113.200]: <victim@example.org>
May 03 15:22:12 mail01 postfix/smtp[902]: 9A8B1: to=<victim@example.org>, relay=mx.example.org
```

### 판단

- 외부 unknown 클라이언트가 외부 수신자에게 메일 릴레이를 시도합니다.
- 인증 없이 릴레이가 허용되면 스팸 발송지로 악용됩니다.

### 답안 예시

```text
외부 IP가 메일 서버를 통해 외부 도메인으로 메일을 릴레이하는 정황이 있어 오픈 릴레이 설정이 의심된다.
오픈 릴레이는 스팸 발송, 블랙리스트 등록, 서비스 평판 하락을 초래할 수 있다.
릴레이 허용 대상을 내부 IP 또는 인증 사용자로 제한하고 SMTP 인증, TLS, 발송량 제한, 메일 큐 점검, 스팸 발송 로그 분석을 수행한다.
```

## 케이스 10. DB 대량 조회와 반출 의심

### 로그

```text
2026-05-03 02:01:11 user=admin ip=10.0.5.20 action=SELECT table=customer rows=120
2026-05-03 02:02:40 user=admin ip=10.0.5.20 action=SELECT table=customer rows=120000
2026-05-03 02:04:03 user=admin ip=10.0.5.20 action=EXPORT table=customer file=/tmp/customer.csv
```

### 판단

- 야간에 관리자 계정으로 대량 조회와 내보내기가 수행됩니다.
- 개인정보 또는 고객정보 반출 사고 가능성이 있습니다.

### 답안 예시

```text
야간 시간대 관리자 계정으로 고객 테이블 대량 조회와 파일 내보내기가 발생하여 내부자 또는 탈취 계정에 의한 정보 반출이 의심된다.
즉시 해당 세션을 종료하고 계정 잠금 또는 비밀번호 변경을 수행하며 DB 감사 로그, OS 파일 생성 이력, 네트워크 반출 로그를 보존한다.
반출 파일의 존재와 외부 전송 여부를 확인하고, 관리자 MFA, DB 접근권한 최소화, 대량조회 탐지, DLP 연계, 정기 권한 검토를 적용한다.
```

## 케이스 11. VPN 이상 접속

### 로그

```text
2026-05-03 09:00:01 vpn user=kim result=success src=203.0.113.11 country=KR
2026-05-03 09:07:44 vpn user=kim result=success src=198.51.100.44 country=US
2026-05-03 09:08:10 fw src=vpn-pool-10.8.0.34 dst=10.0.10.20 service=RDP action=allow
```

### 판단

- 같은 계정이 짧은 시간 안에 다른 국가에서 성공 로그인합니다.
- VPN 접속 후 내부 RDP 접근까지 이어집니다.

### 답안 예시

```text
동일 계정이 짧은 시간 내 서로 다른 국가에서 VPN 접속에 성공하고 내부 RDP 접근이 확인되어 계정 탈취가 의심된다.
즉시 해당 계정 세션을 종료하고 비밀번호 변경, MFA 재등록, 접속 IP 차단을 수행한다.
VPN 접속 로그와 내부 접근 로그를 상관분석하여 접근 범위를 확인하고, 지리적 이상접속 탐지, MFA 의무화, 단말 보안상태 검사, 내부 접근권한 최소화를 적용한다.
```

## 케이스 12. 랜섬웨어 확산

### 단서

```text
2026-05-03 16:10:01 file01 event=rename path=\\share\team\report.docx -> report.docx.locked user=park
2026-05-03 16:10:02 file01 event=rename path=\\share\team\budget.xlsx -> budget.xlsx.locked user=park
2026-05-03 16:10:03 file01 event=rename path=\\share\team\contract.pdf -> contract.pdf.locked user=park
2026-05-03 16:10:04 edr host=PC-23 detection=ransomware_behavior action=alert
```

### 판단

- 짧은 시간에 공유폴더 파일이 일괄적으로 특정 확장자로 변경됩니다.
- EDR에서 랜섬웨어 행위 탐지가 발생했습니다.

### 답안 예시

```text
짧은 시간에 다수 파일이 .locked 확장자로 변경되고 EDR에서 랜섬웨어 행위가 탐지되어 랜섬웨어 감염과 공유폴더 확산이 의심된다.
감염 PC를 네트워크에서 격리하고 해당 계정의 공유폴더 접근을 차단하며, 파일서버 스냅샷과 백업 상태를 확인한다.
감염 경로를 분석하고 안전한 백업으로 복구하며, 최소권한, 네트워크 분리, EDR 차단정책, 메일 보안, 불변 백업과 복구훈련을 강화한다.
```

## 케이스 13. WAF 탐지와 실제 성공 여부

### 로그

```text
WAF: time=2026-05-03T17:00:01 src=203.0.113.99 uri=/product?id=1%20or%201=1 rule=SQLI-001 action=block
WEB: 203.0.113.99 - - [03/May/2026:17:00:01 +0900] "GET /product?id=1%20or%201=1 HTTP/1.1" 403 512
WEB: 203.0.113.99 - - [03/May/2026:17:00:05 +0900] "GET /product?id=2 HTTP/1.1" 200 3100
```

### 판단

- WAF가 SQL Injection 룰로 차단했고 웹 서버도 403을 반환했습니다.
- 정상 요청 200과 공격 요청 403을 구분해야 합니다.

### 답안 예시

```text
WAF와 웹 서버 로그에서 SQL Injection 패턴 요청이 차단되어 공격 시도는 있었으나 해당 요청은 403으로 처리되어 차단된 것으로 보인다.
다만 우회 시도와 다른 파라미터 공격 가능성을 확인하기 위해 동일 출발지의 전체 요청과 DB 로그를 상관분석한다.
WAF 룰을 최신화하고 취약 파라미터에 Prepared Statement가 적용되어 있는지 재점검한다.
```

## 케이스 14. 보안장비 로그 시간 불일치

### 로그

```text
FW  : 2026-05-03 10:00:01 allow src=203.0.113.10 dst=10.0.0.10 dpt=443
WAF : 2026-05-03 10:15:02 detect src=203.0.113.10 uri=/login
WEB : 2026-05-03 10:00:03 POST /login 500
```

### 판단

- 같은 흐름으로 보이지만 WAF 시간이 15분 어긋나 있습니다.
- 상관분석 정확도가 떨어집니다.

### 답안 예시

```text
방화벽, WAF, 웹 서버 로그의 시간 기준이 일치하지 않아 사고 흐름 분석과 증거 신뢰성에 문제가 생길 수 있다.
각 장비의 NTP 설정과 시간대를 확인하고 중앙 시간 서버로 동기화한다.
기존 분석 시에는 시간 오차를 보정하여 상관분석하고, 이후 로그 수집 시스템에서 시간 동기화 상태를 모니터링한다.
```

## 패킷 문제에서 자주 묻는 TCP 플래그

| 플래그 | 의미 | 시험 포인트 |
| --- | --- | --- |
| SYN | 연결 시작 | SYN Flood, SYN Scan |
| ACK | 확인 응답 | 정상 연결 성립 여부 |
| FIN | 연결 종료 | FIN Scan |
| RST | 연결 강제 종료 | 닫힌 포트 응답, 비정상 종료 |
| PSH | 즉시 전달 요청 | 데이터 전송 흐름 |
| URG | 긴급 데이터 | Xmas Scan 조합에서 등장 가능 |

## 상태코드 빠른 판단

| 코드 | 의미 | 보안 분석 힌트 |
| --- | --- | --- |
| 200 | 성공 | 공격 요청이 성공했는지 확인 필요 |
| 301/302 | 리다이렉트 | 인증 우회/피싱 판단에는 추가 확인 필요 |
| 400 | 잘못된 요청 | 스캐너나 깨진 페이로드 가능 |
| 401 | 인증 필요 | 인증 실패 반복과 연결 |
| 403 | 권한 없음/차단 | WAF나 서버 정책 차단 가능 |
| 404 | 없음 | 경로 탐색, 스캐닝 가능 |
| 500 | 서버 오류 | SQLi, 경로조작, 예외 노출 가능 |

## 실기에서 답안을 망치는 흔한 실수

| 실수 | 왜 감점되는가 | 보완 문장 |
| --- | --- | --- |
| 공격명만 씀 | 근거가 없어 추측처럼 보임 | `로그의 [필드]에서 [패턴]이 확인된다` |
| 차단만 씀 | 원인 제거와 재발방지가 빠짐 | `차단 후 취약 설정을 수정하고 재점검한다` |
| 로그 삭제를 조치로 씀 | 증거 훼손 위험 | `원본 로그를 보존하고 접근권한을 제한한다` |
| 암호화만 반복 | 접근통제, 패치, 모니터링 누락 | `암호화와 함께 키관리, 접근권한, 로그점검을 적용한다` |
| 법규를 단정 | 최신 조문 확인이 필요 | `현행 법령 기준으로 신고/통지 요건을 확인한다` |

## 미니 훈련

아래 단서를 보고 공격명과 대응을 직접 써봅니다.

1. `GET /?page=http://169.254.169.254/latest/meta-data/`
2. `GET /admin.php`, `GET /phpmyadmin/`, `GET /.env`가 1초 간격으로 반복
3. `POST /transfer` 요청에 Referer가 외부 사이트이고 CSRF 토큰이 없음
4. VPN 성공 로그인 직후 내부 DB 서버 3306 포트 접근
5. DNS 서버에 외부 IP의 재귀 질의가 대량 발생
6. 방화벽 정책에 `any any allow`가 상단에 존재
7. DB 감사 로그에서 `EXPORT`가 야간에 반복
8. 웹 오류 로그에 SQL 문과 테이블명이 그대로 노출

정답 방향:

| 번호 | 의심 | 핵심 대응 |
| --- | --- | --- |
| 1 | SSRF | 내부/메타데이터 주소 차단, URL 허용목록 |
| 2 | 웹 스캐닝/정보노출 탐색 | 차단, 불필요 파일 제거, 관리자 경로 보호 |
| 3 | CSRF | CSRF 토큰, SameSite, 재인증 |
| 4 | 탈취 VPN 계정 내부 이동 | 세션 종료, MFA, 접근범위 제한, 로그 상관분석 |
| 5 | 오픈 리졸버/DNS 증폭 | 재귀 질의 내부 제한, rate limit |
| 6 | 방화벽 정책 오류 | 최소허용 정책, 정책 순서 검토, 만료일 관리 |
| 7 | 내부자/탈취 계정 반출 | 계정 조치, DLP, 대량조회 탐지, 증거 보존 |
| 8 | 오류 정보 노출 | 일반 오류 메시지, 상세 오류 내부 로그 처리 |
