# 04. 정보보안일반

## 한눈에 보는 핵심

정보보안일반은 보안의 기본 이론 과목입니다. 인증, 접근통제, 전자서명, 공개키 기반 구조, 암호, 해시, 메시지 인증을 묻습니다. 용어가 비슷하므로 `목적`, `제공하는 보안성`, `대표 방식`, `한계`를 함께 외워야 합니다.

## 정보보호 3요소

- 기밀성: 허가된 사람만 정보를 볼 수 있게 합니다.
- 무결성: 정보가 허가 없이 변경되지 않게 합니다.
- 가용성: 필요한 때 정보와 서비스를 사용할 수 있게 합니다.

추가로 책임추적성, 인증성, 부인방지도 자주 함께 나옵니다.

## 인증

### 사용자 인증

사용자가 주장하는 신원이 맞는지 확인합니다.

- 지식 기반: 비밀번호, PIN
- 소유 기반: OTP 토큰, 인증서, 보안카드
- 생체 기반: 지문, 얼굴, 홍채
- 위치/행위 기반: 접속 위치, 사용 패턴
- 다중인증: 서로 다른 인증 요소를 조합해 보안을 강화합니다.

### 인증 방식 비교

| 방식 | 장점 | 위험 |
| --- | --- | --- |
| 비밀번호 | 쉽고 저렴함 | 탈취, 재사용, 대입 공격 |
| OTP | 재사용 위험 감소 | 피싱, 단말 탈취 |
| 인증서 | 전자서명과 연계 가능 | 개인키 유출 |
| 생체인증 | 사용 편의성 | 변경 불가 정보 유출 위험 |

### 메시지 인증

메시지가 중간에 변경되지 않았고 정당한 송신자로부터 왔는지 확인합니다.

- 메시지 인증 코드
- 전자서명
- 해시와 키를 조합한 인증 방식

### 디바이스 인증

단말이나 장비가 신뢰할 수 있는 대상인지 확인합니다.

- 인증서 기반 장비 인증
- 사전 공유키
- 장비 고유 식별자
- 안전한 저장소와 원격 검증

## 접근통제

### 기본 구성요소

- 주체: 접근을 요청하는 사용자, 프로세스, 시스템
- 객체: 파일, DB, 네트워크, 프로그램 같은 보호 대상
- 접근권한: 읽기, 쓰기, 실행, 삭제, 관리
- 정책: 접근 허용과 거부 기준
- 참조 모니터: 모든 접근 요청을 검사하는 논리적 통제 장치

### 접근통제 모델

| 모델 | 핵심 | 예 |
| --- | --- | --- |
| 임의적 접근통제 | 객체 소유자가 권한 부여 | 파일 소유자의 권한 설정 |
| 강제적 접근통제 | 보안 등급과 정책에 따라 통제 | 군사/기밀 등급 |
| 역할 기반 접근통제 | 직무 역할에 권한 부여 | 인사담당자, 관리자 |
| 속성 기반 접근통제 | 사용자, 자원, 환경 속성으로 판단 | 위치, 시간, 기기 상태 |

### 대표 보안 모델

- Bell-LaPadula: 기밀성 중심. 낮은 등급에서 높은 등급 읽기 금지, 높은 등급에서 낮은 등급 쓰기 금지.
- Biba: 무결성 중심. 낮은 무결성에서 높은 무결성 쓰기 금지, 높은 무결성에서 낮은 무결성 읽기 금지.
- Clark-Wilson: 무결성 중심. 잘 형성된 트랜잭션과 직무분리를 강조합니다.

## 키 분배

### 대칭키 기반

송신자와 수신자가 같은 비밀키를 공유합니다.

- 장점: 빠릅니다.
- 단점: 사용자 수가 많아지면 키 관리가 어려워집니다.
- 대표 방식: 키 분배 센터, Kerberos

### 공개키 기반

공개키와 개인키 쌍을 사용합니다.

- 공개키: 누구에게나 공개 가능
- 개인키: 소유자만 안전하게 보관
- 장점: 키 분배 문제가 줄어듭니다.
- 단점: 대칭키보다 연산이 느립니다.
- 활용: 전자서명, 키 교환, 인증서

### Diffie-Hellman 키 교환

서로 비밀값을 직접 보내지 않고 공통 세션키를 만들 수 있습니다. 단독으로 사용하면 중간자 공격에 취약하므로 인증과 함께 사용해야 합니다.

## 전자서명

### 목적

- 무결성: 문서가 변경되지 않았음을 확인
- 인증: 서명자가 누구인지 확인
- 부인방지: 나중에 서명 사실을 부인하기 어렵게 함

### 원리

1. 문서의 해시값을 계산합니다.
2. 해시값을 서명자의 개인키로 서명합니다.
3. 검증자는 서명자의 공개키로 서명을 확인하고 문서 해시와 비교합니다.

### 전자서명과 암호화 차이

- 전자서명: 개인키로 서명하고 공개키로 검증합니다.
- 공개키 암호화: 수신자의 공개키로 암호화하고 수신자의 개인키로 복호화합니다.

## 공개키 기반 구조

### 구성요소

- 인증기관: 인증서를 발급하고 신뢰의 기준이 됩니다.
- 등록기관: 신원확인과 등록 업무를 보조합니다.
- 인증서: 공개키와 소유자 정보를 묶고 인증기관이 서명한 문서입니다.
- 저장소: 인증서와 폐지 정보를 제공합니다.
- 인증서 폐지목록: 폐지된 인증서 목록입니다.
- 온라인 인증서 상태 확인: 특정 인증서의 현재 상태를 실시간으로 확인합니다.

### 인증서 검증

- 인증서 유효기간 확인
- 인증기관 서명 확인
- 인증서 체인 확인
- 폐지 여부 확인
- 도메인 또는 주체 정보 확인

### 은닉서명과 이중서명

- 은닉서명: 서명자가 실제 메시지 내용을 보지 않고도 서명할 수 있게 하는 방식입니다. 전자화폐 같은 익명성이 필요한 구조에서 언급됩니다.
- 이중서명: 서로 다른 두 정보를 연결하되 각 당사자는 필요한 정보만 볼 수 있게 하는 방식입니다. 결제 정보와 주문 정보를 분리하는 전자상거래 구조에서 다룹니다.

## 암호학 기초

### 용어

- 평문: 암호화 전 데이터
- 암호문: 암호화 후 데이터
- 암호화: 평문을 암호문으로 바꾸는 과정
- 복호화: 암호문을 평문으로 되돌리는 과정
- 키: 암호화와 복호화에 쓰이는 비밀값
- 알고리즘: 암호 처리 절차

### 암호 공격

- 암호문 단독 공격: 암호문만 가지고 평문이나 키를 추정
- 알려진 평문 공격: 일부 평문과 암호문 쌍을 알고 공격
- 선택 평문 공격: 공격자가 선택한 평문에 대한 암호문을 얻어 공격
- 선택 암호문 공격: 공격자가 선택한 암호문에 대한 평문을 얻어 공격
- 전수조사 공격: 가능한 키를 모두 시도
- 생일 공격: 해시 충돌 가능성을 이용
- 중간자 공격: 키 교환 과정에 끼어들어 양쪽을 속임
- 부채널 공격: 시간, 전력, 오류 같은 구현 정보를 이용

## 대칭키 암호

### 특징

- 같은 키로 암호화와 복호화를 수행합니다.
- 빠르고 대용량 데이터 암호화에 적합합니다.
- 키를 안전하게 공유하는 문제가 있습니다.

### 블록 암호와 스트림 암호

- 블록 암호: 고정 크기 블록 단위로 처리합니다.
- 스트림 암호: 키스트림과 평문을 결합해 처리합니다.

### 대표 알고리즘

- DES: 오래된 표준으로 현재는 안전하지 않습니다.
- 3DES: DES를 세 번 적용하지만 성능과 보안 한계가 있습니다.
- AES: 널리 쓰이는 표준 블록 암호입니다.
- SEED, ARIA: 국내에서 사용되는 블록 암호입니다.

### 운영 모드

| 모드 | 특징 | 주의 |
| --- | --- | --- |
| ECB | 같은 평문 블록이 같은 암호문이 됨 | 패턴 노출로 사용 지양 |
| CBC | 이전 암호문 블록과 결합 | 초기화 벡터 관리 필요 |
| CFB | 스트림처럼 사용 가능 | 오류 전파 특성 이해 |
| OFB | 키스트림 생성 | 초기화 벡터 재사용 금지 |
| CTR | 카운터 기반 | 병렬 처리 가능, 카운터 재사용 금지 |
| GCM | 암호화와 인증 제공 | 초기화 벡터 재사용 금지 |

## 공개키 암호

### 특징

- 공개키와 개인키를 사용합니다.
- 키 분배와 전자서명에 유리합니다.
- 대칭키보다 느려서 보통 세션키 교환이나 서명에 사용합니다.

### 인수분해 기반

- RSA가 대표적입니다.
- 큰 수의 소인수분해가 어렵다는 성질을 기반으로 합니다.
- 암호화와 전자서명에 모두 활용됩니다.

### 이산로그 기반

- Diffie-Hellman, ElGamal, DSA, 타원곡선 방식이 관련됩니다.
- 같은 보안 수준에서 타원곡선 방식은 키 길이를 줄일 수 있습니다.

## 해시함수

### 요구사항

- 임의 길이 입력을 고정 길이 출력으로 변환
- 같은 입력은 항상 같은 출력
- 출력만으로 입력을 찾기 어려움
- 같은 해시를 갖는 두 입력을 찾기 어려움
- 입력이 조금만 바뀌어도 출력이 크게 바뀜

### 활용

- 무결성 검증
- 비밀번호 저장
- 전자서명 전 요약
- 파일 위변조 탐지
- 메시지 인증 코드 구성

### 주의

- MD5와 SHA-1은 충돌 취약점으로 일반적인 보안 목적에 부적합합니다.
- 비밀번호 저장에는 단순 해시가 아니라 솔트와 느린 해시가 필요합니다.

## 메시지 인증 코드

메시지 인증 코드는 공유 비밀키를 이용해 메시지 무결성과 송신자 인증을 제공합니다.

- HMAC: 해시함수를 기반으로 한 메시지 인증 코드
- CMAC: 블록 암호 기반 메시지 인증 코드
- 전자서명과 달리 송수신자가 같은 키를 공유하므로 부인방지는 약합니다.

## 난수와 키 관리

- 암호키는 충분한 난수성으로 생성해야 합니다.
- 키는 용도별로 분리합니다.
- 키 저장소 접근권한을 제한합니다.
- 키 교체와 폐기 절차가 필요합니다.
- 개발 코드와 저장소에 키를 직접 넣지 않습니다.

## 자주 헷갈리는 비교

| 항목 | 핵심 차이 |
| --- | --- |
| 암호화와 해시 | 암호화는 복호화 가능, 해시는 원칙적으로 복원 불가 |
| 해시와 MAC | 해시는 키가 없고, MAC은 비밀키를 사용 |
| MAC과 전자서명 | MAC은 공유키, 전자서명은 개인키/공개키 |
| 기밀성과 무결성 | 기밀성은 숨김, 무결성은 변경 방지 |
| 인증과 부인방지 | 인증은 신원 확인, 부인방지는 행위 부정 방지 |

